# 2026-04-08 主窗口默认居中启动

## 本次目标
- 将应用主窗口默认启动位置调整为屏幕居中展示。

## 核心改动
- 在 `MainWindow` 上增加 `WindowStartupLocation="CenterScreen"`。
- 结合当前仓库结构确认：系统设置目前是主窗口中的页面入口，而不是独立设置窗口，因此本次调整落在主窗口层面。

## 修改文件
- `AzrngTools/Views/MainWindow.axaml`
- `TASK.md`

## 校验情况
- 已执行 `dotnet build AzrngTools.sln`
- 构建通过，0 错误
- 构建过程中存在 2 条 `MSB3026` 警告：因本地运行中的 `AzrngTools.exe` 占用了输出文件

## 风险或遗留项
- 本次只调整主窗口默认启动位置，未改动数据库模块等已单独设置为 `CenterOwner` 的弹窗行为。
