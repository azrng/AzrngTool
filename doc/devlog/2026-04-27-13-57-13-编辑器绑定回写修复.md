# 本次目标
- 修复 JSON 等文本页编辑器内容未稳定回写到 ViewModel，导致点击命令时误判空输入的问题

# 核心改动
- 调整 `TextEditorBinding.SetText`，由 `SetValue` 改为 `SetCurrentValue`
- 保留 `TextEditorBinding` 的双向绑定关系，避免编辑器文本变化时把原有绑定覆盖成局部值
- 该修复同时作用于使用 `TextEditorBinding` 的 JSON、字符串、SQL、JWT 等同类页面
- 补充 `.learnings/ERRORS.md`，记录当前测试栈下无法直接实例化 `AvaloniaEdit.TextEditor` 的限制

# 修改文件
- `AzrngTools/Behaviors/TextEditorBinding.cs`
- `.learnings/ERRORS.md`
- `TASK.md`
- `doc/devlog/2026-04-27-13-57-13-编辑器绑定回写修复.md`

# 校验情况
- 已执行：`dotnet test C:\Work\github\AzrngTool\AzrngTools.Tests\AzrngTools.Tests.csproj -c Debug`
- 校验结果：`14` 个测试全部通过，`0` 失败，`0` 跳过
- 已执行：`dotnet build C:\Work\github\AzrngTool\AzrngTools.sln -c Release`
- 校验结果：构建通过，`0` 警告，`0` 错误

# 风险或遗留项
- 当前未补 `TextEditorBinding` 的行为层自动化测试；原因是现有测试栈缺少可稳定实例化 `AvaloniaEdit.TextEditor` 的 UI 上下文
- 若后续需要继续覆盖这类编辑器交互，建议引入 Avalonia Headless 或专门的 UI 测试上下文
