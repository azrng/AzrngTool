# 本次目标
- 修复 `JSON格式化` 页面处理 JSON Schema 时，对同时包含 `$ref` 与 `$schema` 的合法输入误报解析失败的问题

# 核心改动
- 调整 `JsonHelper.JsonFormatter` 的解析方式，改为基于 `JToken.Parse` 读取 JSON，再按缩进格式输出
- 移除对 `DeserializeObject<object>` 的依赖，避免 `Newtonsoft.Json` 将 `$ref` 误判为引用元数据对象

# 修改文件
- `AzrngTools/Utils/JsonHelper.cs`
- `TASK.md`
- `doc/devlog/2026-04-27-10-44-49-JSON格式化引用对象报错修复.md`

# 校验情况
- 已执行：`dotnet build C:\Work\github\AzrngTool\AzrngTools.sln -c Debug`
- 校验结果：构建通过，`0` 警告，`0` 错误
- 已执行：`dotnet run --project C:\Users\Public\AzrngToolJsonVerify\AzrngToolJsonVerify.csproj`
- 校验结果：可正常格式化包含 `$ref` 与 `$schema` 的示例 JSON Schema，不再报 “A JSON reference object should only have a $ref property”

# 风险或遗留项
- 本次做的是底层格式化路径修复，未重新打开桌面应用做人工界面点检
- 临时验证项目位于 `C:\Users\Public\AzrngToolJsonVerify`，仅用于本次命令行等价验证
