# PDF加载后操作禁用修复

- 本次目标：修复 PDF 管理页选择 PDF 后仍只显示 Aspose 评估模式提示、分割和导出 Word 操作保持禁用的问题。
- 核心改动：`PdfManagementPageViewModel.LoadPdfAsync` 将成功结果但 `Data` 为空的情况显式视为加载失败，避免页面进入无当前文件的含混状态；成功加载后直接使用文件摘要刷新页码范围、默认输出路径、授权提示和命令可用性；补充 ViewModel 回归测试覆盖加载后 `CanOperate` 恢复和空摘要失败分支。
- 修改文件：`AzrngTools/ViewModels/Pdf/PdfManagementPageViewModel.cs`、`AzrngTools.Tests/ViewModels/Pdf/PdfManagementPageViewModelTests.cs`、`TASK.md`。
- 校验情况：`dotnet test AzrngTools.Tests/AzrngTools.Tests.csproj --filter "FullyQualifiedName~PdfManagementPageViewModelTests" -v minimal -p:BaseOutputPath="C:\Temp\AzrngTool-test-bin\"` 通过，5 个测试通过；`dotnet test AzrngTools.Tests/AzrngTools.Tests.csproj --filter "FullyQualifiedName~Pdf" -v minimal -p:BaseOutputPath="C:\Temp\AzrngTool-test-bin\"` 通过，14 个测试通过；`dotnet build AzrngTools/AzrngTools.csproj -v minimal -p:BaseOutputPath="C:\Temp\AzrngTool-build-bin\"` 通过；`dotnet build AzrngTools/AzrngTools.csproj -c Release -v minimal -p:BaseOutputPath="C:\Temp\AzrngTool-release-bin\"` 通过。
- 风险或遗留项：常规 Debug 输出目录当前被正在运行的 `AzrngTools (19624)` 锁定，验证使用临时输出目录绕开；未关闭用户当前打开的应用；人工 UI PDF 分割、导出 Word smoke test 仍未完成。
