# 查询预览默认上限调整

## 本次目标
- 将数据库工作台 SQL 查询预览默认限制从 1000 行调整为 500 行。

## 核心改动
- 在 `DatabaseQueryPreviewLimiter` 中暴露默认预览行数常量 `DefaultMaxRows = 500`。
- `DatabaseService` 使用统一默认值生成预览查询和用户提示。
- 更新查询限流单元测试，锁定默认上限为 500。

## 修改文件
- `AzrngTools/Services/Database/DatabaseQueryPreviewLimiter.cs`
- `AzrngTools/Services/Database/DatabaseService.cs`
- `AzrngTools.Tests/Services/Database/DatabaseQueryPreviewLimiterTests.cs`
- `TASK.md`

## 校验情况
- 已执行针对性测试：`dotnet test AzrngTools.Tests\AzrngTools.Tests.csproj --no-restore --filter "FullyQualifiedName~DatabaseQueryPreviewLimiterTests"`，5 个测试通过。

## 风险或遗留项
- 仅调整默认限制值，不改变 SQL 改写策略。
