# 本次目标
- 为 `JSON格式化` 页面相关 helper 与关键 ViewModel 行为补充最小单元测试覆盖

# 核心改动
- 新增 `AzrngTools.Tests` xUnit 测试工程，并接入 `AzrngTools.sln`
- 为 `JsonHelper` 补充格式化、压缩、转义、去转义与异常分支测试
- 为 `JsonPageViewModel` 补充 `ReplaceEscape` 成功 / 失败路径和空输入提示测试
- 新增测试替身 `TestMessageService`，用于断言 ViewModel 消息提示行为
- 补充 `.learnings/ERRORS.md`，记录 `dotnet new` 模板不接受 `net10.0-windows` 参数的问题

# 修改文件
- `AzrngTools.sln`
- `AzrngTools.Tests/AzrngTools.Tests.csproj`
- `AzrngTools.Tests/TestDoubles/TestMessageService.cs`
- `AzrngTools.Tests/Utils/JsonHelperTests.cs`
- `AzrngTools.Tests/ViewModels/Format/JsonPageViewModelTests.cs`
- `.learnings/ERRORS.md`
- `TASK.md`
- `doc/devlog/2026-04-27-12-21-52-JSON页单元测试补齐.md`

# 校验情况
- 已执行：`dotnet test C:\Work\github\AzrngTool\AzrngTools.Tests\AzrngTools.Tests.csproj -c Debug`
- 校验结果：`10` 个测试全部通过，`0` 失败，`0` 跳过
- 已覆盖：JSON Schema 格式化、合法 JSON 压缩、非法 JSON 压缩异常、文本转义、两类 JSON 去转义、去转义失败保留原文、空输入提示

# 风险或遗留项
- 当前测试主要聚焦 `JsonHelper` 和 `JsonPageViewModel`，尚未覆盖 JSON 页的 Avalonia 视图层绑定与交互
- 仓库目前仍未建立更广泛的测试分层约定；后续若继续扩展测试，建议明确 `Utils`、`ViewModels`、集成测试目录规范
