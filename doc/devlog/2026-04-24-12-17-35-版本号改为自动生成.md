# 本次目标
- 参考 `ApixPress` 将当前项目版本号切换为自动生成
- 默认不再需要手动维护根目录 `VERSION` 的具体版本值，同时保留显式版本兼容能力

# 核心改动
- 新增根级 `Directory.Build.props`，统一生成 `Version`、`InformationalVersion`、`AssemblyVersion`、`FileVersion`
- 将 `AzrngTools/AzrngTools.csproj` 中直接读取根目录 `VERSION` 的版本逻辑移除，改为复用根级公共属性
- 调整 `.github/workflows/release.yml`，当 `VERSION` 为 `auto`、为空或不存在时自动使用当天日期版本；若为显式版本号则继续沿用
- 将工作流发布时的 `InformationalVersion` 收口为纯版本号，并显式透传 `AzrngToolsVersionDate`
- 将根目录 `VERSION` 改为 `auto`
- 更新 `README.md` 中的发布流程与版本规则说明

# 修改文件
- `Directory.Build.props`
- `AzrngTools/AzrngTools.csproj`
- `.github/workflows/release.yml`
- `VERSION`
- `README.md`
- `TASK.md`
- `doc/devlog/2026-04-24-12-17-35-版本号改为自动生成.md`

# 校验情况
- 已执行：`dotnet msbuild AzrngTools\AzrngTools.csproj -getProperty:Version -getProperty:InformationalVersion -getProperty:AssemblyVersion -getProperty:FileVersion -getProperty:IncludeSourceRevisionInInformationalVersion`
- 校验结果：默认自动解析为 `2026.4.24`，且 `IncludeSourceRevisionInInformationalVersion=false`
- 已执行：`dotnet msbuild AzrngTools\AzrngTools.csproj -p:AzrngToolsLegacyVersion=2026.4.9 -getProperty:Version -getProperty:InformationalVersion -getProperty:AssemblyVersion -getProperty:FileVersion`
- 校验结果：显式版本覆盖后输出为 `2026.4.9 / 2026.4.9.0`
- 已执行：`dotnet build AzrngTools.sln -c Release -v minimal`
- 已执行：`dotnet publish AzrngTools\AzrngTools.csproj -c Release -r win-x64 --self-contained true -o publish\win-x64-auto-version-test -p:PublishAot=false -p:PublishTrimmed=false -p:DebugType=None -p:DebugSymbols=false -v minimal`
- 已执行：读取 `publish\win-x64-auto-version-test\AzrngTools.exe` 的 `ProductVersion` 与 `FileVersion`
- 校验结果：`ProductVersion=2026.4.24`，`FileVersion=2026.4.24.0`
- 已执行：启动 `publish\win-x64-auto-version-test\AzrngTools.exe` 并在 5 秒后确认进程存活，随后手动结束进程

# 风险或遗留项
- 若后续把 `VERSION` 再改成显式数字版本，构建与发布会优先使用该显式值，不会继续自动按日期生成
- 本次未实际触发 GitHub Actions 远端发布，只完成了本地等价验证
