# Base64 文本页结果区高度调整

## 本次目标
- 删除 Base64 文本页左侧输入建议区域。
- 将右侧处理结果输入框高度调高到与左侧输入框一致。

## 核心改动
- 移除 `Base64EncodePageView` 左侧输入卡片底部的输入建议说明块。
- 将右侧处理结果 `TextBox` 高度从 360 调整为 500，与左侧输入 `TextBox` 保持一致。

## 修改文件
- `AzrngTools/Views/Encode/Base64EncodePageView.axaml`
- `TASK.md`
- `doc/devlog/2026-05-20-14-39-35-Base64文本页结果区高度调整.md`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln`，构建成功，0 警告，0 错误。
- 已执行：重新读取修改后的 XAML、`TASK.md` 与本开发记录，关键中文显示正常。
- 未执行：人工打开 Base64 文本页进行界面 smoke test；本次为布局微调，已通过构建和文件回读做等价校验。

## 风险或遗留项
- 本次仅调整 Base64 文本页布局，不涉及 ViewModel 和业务逻辑。
