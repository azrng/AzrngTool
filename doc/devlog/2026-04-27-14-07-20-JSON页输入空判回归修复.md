# 本次目标
- 修复 JSON 页面编辑器已有内容但命令仍提示“请输入要处理的内容”的回归问题

# 核心改动
- 在 `JsonPageView` 中恢复页面级 `TextChanged` 回写兜底
- 将编辑器内容同步的订阅与解绑放到页面 `AttachedToVisualTree` / `DetachedFromVisualTree` 生命周期中
- 保留此前 `TextEditorBinding.SetCurrentValue` 的共享绑定修复，页面级兜底只用于确保 JSON 页当前行为立即回稳
- 补充 `.learnings/ERRORS.md`，记录当前 `Debug` 配置下 `WithDeveloperTools()` 无法解析的问题

# 修改文件
- `AzrngTools/Views/Format/JsonPageView.axaml.cs`
- `.learnings/ERRORS.md`
- `TASK.md`
- `doc/devlog/2026-04-27-14-07-20-JSON页输入空判回归修复.md`

# 校验情况
- 已执行：`dotnet test C:\Work\github\AzrngTool\AzrngTools.Tests\AzrngTools.Tests.csproj -c Release`
- 校验结果：`14` 个测试全部通过，`0` 失败，`0` 跳过
- 已执行：`dotnet build C:\Work\github\AzrngTool\AzrngTools.sln -c Release`
- 校验结果：构建通过，`0` 警告，`0` 错误
- 未执行：`dotnet test ... -c Debug`
- 原因：当前 `Debug` 配置会因 `Program.cs` 中 `WithDeveloperTools()` 无法解析而失败，已单独记录到 `.learnings/ERRORS.md`

# 风险或遗留项
- 当前 JSON 页同时保留共享绑定修复和页面级回写兜底，后续若要彻底收口，需要先解决 `TextEditorBinding` 在 UI 测试环境中的可验证性，再决定是否移除页面级逻辑
