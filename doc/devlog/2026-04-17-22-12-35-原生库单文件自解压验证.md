# 本次目标
- 验证 Avalonia 原生库是否可以随单文件一起打包，而不是继续外置在发布目录
- 尝试使用 .NET 官方单文件原生库自解压方案，确认 Windows 下能否稳定启动

# 核心改动
- 在 `AzrngTools.csproj` 的 Release 配置中增加 `IncludeNativeLibrariesForSelfExtract=true`
- 保留现有 `Costura.Fody` 对 `Microsoft.Data.SqlClient.SNI.dll`、`msalruntime.dll`、`WebView2Loader.dll` 的合并策略
- 更新 `README.md`，将发布说明改为“单文件 + 运行时自解压原生库”

# 修改文件
- `AzrngTools/AzrngTools.csproj`
- `README.md`
- `TASK.md`
- `doc/devlog/2026-04-17-22-12-35-原生库单文件自解压验证.md`

# 校验情况
- 已执行：`dotnet publish AzrngTools\AzrngTools.csproj -c Release -r win-x64 --self-contained true -o publish\win-x64-selfextract-test-1 -p:PublishAot=false -p:PublishTrimmed=false -p:DebugType=None -p:DebugSymbols=false -v minimal`
- 已执行：启动 `publish\win-x64-selfextract-test-1\AzrngTools.exe`，进程可成功拉起
- 验证结果：测试目录最终仅保留单个 `AzrngTools.exe`

# 风险或遗留项
- 当前单文件方案依赖运行时把原生库解压到临时目录，不属于纯内存加载
- 这次只完成了发布与启动 smoke test，数据库连接、WebView2 相关页面建议在下次正式发版前补一次人工点检
