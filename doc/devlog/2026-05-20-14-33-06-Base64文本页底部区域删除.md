# Base64 文本页底部区域删除

## 本次目标
- 删除 Base64 文本页底部的使用建议与工作状态区域。
- 调整原始输入框高度，让删掉底部内容后的页面空间更多用于输入长文本。

## 核心改动
- 移除 `Base64EncodePageView` 根布局中的第三行底部卡片。
- 将原始输入框高度从 `360` 调整为 `500`。
- 保留右侧结果区与结果回填、复制等现有操作。

## 修改文件
- `AzrngTools/Views/Encode/Base64EncodePageView.axaml`
- `TASK.md`
- `doc/devlog/2026-05-20-14-33-06-Base64文本页底部区域删除.md`

## 校验情况
- 已执行 `dotnet build AzrngTools.sln`，构建成功，0 警告，0 错误。
- 已重新读取修改后的 XAML，确认关键中文显示正常。
- 未执行自动化 UI smoke test；本次仅为单页布局调整，未改变命令、状态流转或数据逻辑。

## 风险或遗留项
- 需要后续人工打开 Base64 文本页确认不同窗口高度下输入区观感是否符合预期。
