# 本次目标
- 为 `Unix时间戳` 页面关键转换、输入校验和状态同步行为补充最小单元测试覆盖

# 核心改动
- 新增 `UnixTimestampPageViewModelTests` 测试文件
- 覆盖秒级时间戳空输入提示和成功转换结果
- 覆盖本地时间转秒级 / 毫秒级时间戳输出
- 覆盖日期选择与时间选择对 `TargetDateTime` 的同步行为

# 修改文件
- `AzrngTools.Tests/ViewModels/Other/UnixTimestampPageViewModelTests.cs`
- `TASK.md`
- `doc/devlog/2026-04-27-12-27-47-Unix时间戳页单元测试补齐.md`

# 校验情况
- 已执行：`dotnet test C:\Work\github\AzrngTool\AzrngTools.Tests\AzrngTools.Tests.csproj -c Debug`
- 校验结果：`14` 个测试全部通过，`0` 失败，`0` 跳过
- 本次新增覆盖：Unix 时间戳秒级转换、空输入提示、本地时间转时间戳、日期/时间选择同步

# 风险或遗留项
- 当前仍未覆盖 `Unix时间戳` 页的复制命令与 Avalonia 视图层交互
- 时间相关断言按当前本地时区计算，已避免写死具体时区常量，但若后续逻辑引入时区配置抽象，测试也需要同步调整
