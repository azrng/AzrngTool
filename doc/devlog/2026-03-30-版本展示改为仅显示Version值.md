# 2026-03-30 版本展示改为仅显示Version值

## 本次目标
- 修正关于页版本信息展示
- 页面仅显示 `csproj` 中 `<Version>` 对应的版本值，不显示附加哈希

## 核心改动
- 调整 `AppInfoService` 的版本读取逻辑
- 不再读取 `InformationalVersion`
- 改为直接读取程序集版本，避免显示 `+commit hash`

## 修改文件
- `AzrngTools/Services/AppInfoService.cs`
- `TASK.md`
- `doc/devlog/2026-03-30-版本展示改为仅显示Version值.md`

## 校验情况
- 已执行：`dotnet build`
- 结果：通过，0 警告，0 错误

## 风险或遗留项
- 当前版本展示依赖程序集版本生成规则，适用于当前四段纯数字版本号
- 尚未在实际运行界面中手动确认关于页显示值已变为 `2026.3.30.2256`
