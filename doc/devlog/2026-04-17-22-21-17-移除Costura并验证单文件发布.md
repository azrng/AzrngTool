# 本次目标
- 删除 `Costura.Fody` 相关配置与文件
- 验证仅依赖 `.NET` 单文件自解压机制时，发布产物仍能保持单个 exe 且可正常启动

# 核心改动
- 从 `AzrngTools.csproj` 移除 `Costura.Fody` 包引用
- 删除 `AzrngTools/FodyWeavers.xml` 与 `AzrngTools/FodyWeavers.xsd`
- 删除 `.gitignore` 中对 `FodyWeavers.xml` 的白名单
- 保留 `IncludeNativeLibrariesForSelfExtract=true`，并新增仅用于剔除原生调试符号的发布清理目标
- 更新 `README.md`，统一为 `.NET` 官方单文件发布说明

# 修改文件
- `AzrngTools/AzrngTools.csproj`
- `AzrngTools/FodyWeavers.xml`
- `AzrngTools/FodyWeavers.xsd`
- `.gitignore`
- `README.md`
- `TASK.md`
- `doc/devlog/2026-04-17-22-21-17-移除Costura并验证单文件发布.md`

# 校验情况
- 已执行：`dotnet publish AzrngTools\AzrngTools.csproj -c Release -r win-x64 --self-contained true -o publish\win-x64-no-costura-test -p:PublishAot=false -p:PublishTrimmed=false -p:DebugType=None -p:DebugSymbols=false -v minimal`
- 已执行：启动 `publish\win-x64-no-costura-test\AzrngTools.exe`，进程可成功拉起
- 已执行：`dotnet publish AzrngTools\AzrngTools.csproj -c Release -r win-x64 --self-contained true -o publish\win-x64-no-costura-test-2 -p:PublishAot=false -p:PublishTrimmed=false -p:DebugType=None -p:DebugSymbols=false -v minimal`
- 已执行：启动 `publish\win-x64-no-costura-test-2\AzrngTools.exe`，进程可成功拉起
- 验证结果：最终测试目录仅保留单个 `AzrngTools.exe`

# 风险或遗留项
- 当前单文件方案依赖运行时把原生库解压到临时目录，不属于纯内存加载
- 这次只完成了发布与启动 smoke test，数据库连接、WebView2 等相关功能页建议在下次正式发版前补一次人工点检
