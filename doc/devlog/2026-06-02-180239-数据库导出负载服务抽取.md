# 数据库导出负载服务抽取

## 本次目标
- 继续拆分数据库主工作台 ViewModel 中的文档导出负载构建职责。
- 修复导出弹窗可选择视图 / 存储过程，但主导出流程只加载表对象的缺口。

## 核心改动
- 新增 `IDatabaseExportPayloadService`、`DatabaseExportPayloadService` 与 `DatabaseExportPayloadResult`。
- 将导出对象匹配、表字段和索引加载、视图和存储过程加载逻辑移入服务层。
- `MainWindowViewModel` 改为调用导出负载服务，并通过进度回调更新 `LoadingText`。
- 补充导出负载服务测试，覆盖表、视图、存储过程同时选择和未匹配对象分支。
- DI 测试增加导出负载服务解析与单例复用校验。

## 修改文件
- `AzrngTools/Services/Database/IDatabaseExportPayloadService.cs`
- `AzrngTools/Services/Database/DatabaseExportPayloadService.cs`
- `AzrngTools/Services/Database/DatabaseExportPayloadResult.cs`
- `AzrngTools/ViewModels/Database/MainWindowViewModel.cs`
- `AzrngTools.Tests/Services/Database/DatabaseExportPayloadServiceTests.cs`
- `AzrngTools.Tests/Services/Database/DatabaseDependencyInjectionTests.cs`
- `doc/codeReview/2026-06-02-database-module-review.md`
- `TASK.md`

## 校验情况
- 已执行：`dotnet test AzrngTools.Tests\AzrngTools.Tests.csproj`
- 结果：41 个测试通过。

## 风险或遗留项
- 本轮不改变 Excel / Markdown 文件生成格式，只补齐进入导出服务前的负载构建。
- 主 ViewModel 仍保留导出弹窗打开、文件路径选择和导出结果提示等界面编排职责。
