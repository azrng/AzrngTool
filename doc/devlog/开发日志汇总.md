# 开发日志汇总

- 合并日期：2026-04-13
- 原始日志数：104
- 覆盖范围：2026-03-26-Unix时间戳页样式优化.md 至 2026-04-13-13-45-12-首页首屏空白收紧.md
- 说明：以下内容按原文件名升序收录，保留原始记录主体，仅统一为单文件归档。

---

## 2026-04-13 devlog 日志文件合并

来源文件：`开发日志汇总.md`

## 本次目标
- 将 `doc/devlog/` 下分散的历史日志文件合并为单一归档文件，降低目录噪音并保留完整时间线。

## 核心改动
- 新增统一的 `开发日志汇总.md` 作为 devlog 唯一汇总入口。
- 按原文件名升序合并历史 104 份日志内容，保留原始标题、正文结构和校验记录。
- 在汇总文件头部补充本次整理说明，便于后续继续维护单文件日志。

## 修改文件
- `TASK.md`
- `doc/devlog/开发日志汇总.md`

## 校验情况
- 已执行：汇总文件生成后首段与末段内容抽查
- 已执行：统计合并前日志文件数量，确认共收录 104 份历史日志
- 未执行：Markdown 渲染器下的完整人工逐节通读
- 原因：日志总量较大，本次以结构完整性和抽样内容一致性为主

## 风险或遗留项
- 后续若继续沿用单文件模式，`开发日志汇总.md` 会持续增长，必要时可以再按月份拆分为归档文件。

---

## 2026-03-26 Unix时间戳页样式优化

来源文件：`2026-03-26-Unix时间戳页样式优化.md`

## 本次目标
- 优化 Unix 时间戳页面的布局层次与视觉密度
- 解决亮色主题下的文字可见性问题
- 让按钮和对应功能区的关系更清楚
- 收紧底部结果区的留白

## 核心改动
- 将页面结构收敛为 `标题说明 -> 当前时间戳统计 -> 两组主转换区 -> 底部结果区`
- 使用编号和更明确的说明文案区分三大功能块，减少用户判断成本
- 把按钮文案改成动作句，并让按钮紧贴所属输入块，避免出现“按钮不知道在操作哪块内容”的问题
- 将底部区域改为“结果 + 使用说明”双栏布局，避免整页底部出现大面积空白
- 收紧结果区最小高度，让内容密度更接近当前窗口尺寸
- 保留现有功能，不引入新的外部组件

## 修改文件
- `TASK.md`
- `AzrngTools/Views/Other/UnixTimestampPageView.axaml`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln -v minimal`
- 结果：构建成功，0 warning，0 error

## 风险或遗留项
- 当前仅优化 Unix 时间戳页，整个应用的左侧导航和顶部栏仍可继续做统一视觉整理
- 未引入新组件；若后续要引入新的布局/导航组件，会先和用户确认


---

## 2026-03-26 启动链路回稳

来源文件：`2026-03-26-启动链路回稳.md`

## 本次目标
- 排查主题升级后应用无法正常启动的问题，并优先恢复稳定启动能力。

## 核心改动
- 移除 `FluentAvaloniaUI` 依赖，避免额外的主题组件装载风险。
- 保留 `Avalonia Fluent + Semi.Avalonia` 的稳定组合方案。
- 删除 `Program.cs` 中的 `UseHotReload()` 调用，并移除 `HotAvalonia` 包引用。
- 将首页中的 `InfoBar` 改回纯 Avalonia 卡片实现，减少第三方控件参与启动链路。
- 同步更新 `design-system.yaml` 与任务状态。

## 修改文件
- `AzrngTools/AzrngTools.csproj`
- `AzrngTools/Program.cs`
- `AzrngTools/Views/OverviewPageView.axaml`
- `TASK.md`
- `design-system.yaml`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln -v minimal`
- 结果：通过，0 warning / 0 error
- 未执行：真实桌面环境下的窗口持续启动验证
- 原因：当前终端环境无法可靠判断 GUI 进程是否成功呈现在交互桌面

## 风险或遗留项
- 当前已移除最可能影响启动稳定性的新增主题与热重载依赖，但仍需用户在本机桌面环境确认一次实际启动表现。


---

## 2026-03-26 样式系统与 .NET 10 升级

来源文件：`2026-03-26-样式系统与-dotnet10升级.md`

## 本次目标
- 统一项目样式资源，消除页面中的高频硬编码样式
- 将项目目标框架升级到 .NET 10
- 让 AI 规则文件与当前工具箱项目保持一致

## 核心改动
- 新增 `AzrngTools/Styles/DesignTokens.axaml`，集中维护间距、圆角、字号、常用画刷和布局尺寸资源
- 新增 `AzrngTools/Styles/Global.axaml`，统一放置全局样式规则
- 将页面中的高频 `Margin`、`Padding`、`CornerRadius`、`FontSize`、颜色和 `Spacing` 改为资源引用
- 调整 `App.axaml` 的资源接入方式，拆分 token 资源与全局样式
- 将项目目标框架升级到 `net10.0-windows`
- 重写 `AGENTS.md`、`CLAUDE.md`、`GEMINI.md` 与 `design-system.yaml`，使其匹配当前工具箱项目
- 顺手移除 `OverviewPageView` 直接 new ViewModel 的做法，并让主窗口 ViewModel 通过 DI 获取首页实例

## 修改文件
- `TASK.md`
- `AGENTS.md`
- `CLAUDE.md`
- `GEMINI.md`
- `design-system.yaml`
- `AzrngTools/AzrngTools.csproj`
- `AzrngTools/App.axaml`
- `AzrngTools/Styles/DesignTokens.axaml`
- `AzrngTools/Styles/Global.axaml`
- `AzrngTools/ViewModels/MainWindowViewModel.cs`
- `AzrngTools/Views/OverviewPageView.axaml.cs`
- 多个 `AzrngTools/Views/**/*.axaml` 与 `AzrngTools/Themes/*.axaml`
- `AzrngTools/readme.md`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln -v minimal`
- 结果：构建成功，0 warning，0 error
- 已额外检查：被修改的 XAML 页面中不再存在硬编码的高频间距、圆角、字号和十六进制颜色值

## 风险或遗留项
- 当前项目仍有历史架构痕迹，例如部分页面仍未完成更彻底的 Service 化重构
- 本次未引入 `Ursa.Avalonia`，如后续需要复杂导航/对话场景，可单独评估


---

## 2026-03-26 主框架布局整理

来源文件：`2026-03-26-主框架布局整理.md`

## 本次目标
- 优化主窗口壳层布局，解决亮色模式信息层次混乱、内容区留白过大、导航与功能对应关系不清晰的问题。

## 核心改动
- 重构主窗口顶部栏，保留应用标题、副标题和窗口操作，减少噪音信息。
- 将左侧导航拆分为搜索、一级入口和分组工具区，增强功能定位效率。
- 新增页面标题区与副标题区，把当前页面语义前置到内容区域。
- 统一壳层面板、侧栏、页头、导航文本样式，并补充侧栏宽度与内容最大宽度 token。
- 修复菜单搜索后选中项高亮丢失的问题，并修正子菜单 `SelectedItem` 绑定的编译错误。

## 修改文件
- `AzrngTools/Styles/DesignTokens.axaml`
- `AzrngTools/Styles/Global.axaml`
- `AzrngTools/ViewModels/MainWindowViewModel.cs`
- `AzrngTools/Views/MainWindow.axaml`
- `TASK.md`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln -v minimal`
- 结果：通过，0 warning / 0 error
- 未执行：自动化测试、桌面启动 smoke test
- 原因：仓库当前没有测试项目，当前回合未接入可自动收口的桌面 UI 启动验证

## 风险或遗留项
- 当前主题仍基于 `Semi.Avalonia + Fluent` 混合方案，视觉上已经更清晰，但如果后续希望进一步提升品牌感，建议单独做一轮主题体系选型与页面级统一改造。


---

## 2026-03-26 主题体系与首页工作台升级

来源文件：`2026-03-26-主题体系与首页工作台升级.md`

## 本次目标
- 为项目接入更现代的主题底座，修复亮色模式下的可读性问题，并把首页改成更有层次的概览工作台。

## 核心改动
- 引入 `FluentAvaloniaUI`，并将应用主题底座从原生 `FluentTheme` 切换为 `FluentAvaloniaTheme`。
- 同步升级 Avalonia 相关包版本，并保留 `Semi.Avalonia` 作为兼容主题资源层。
- 在 `DesignTokens.axaml` 中新增亮色/暗色语义化颜色 token、应用字体 token 和首页 Hero 尺寸 token。
- 重写 `Global.axaml`，统一窗口、壳层、卡片、导航选中态和概览页样式。
- 调整主窗口左侧导航分段，让“常用入口”和“全部工具”更容易辨识。
- 重做首页 `OverviewPageView`，增加 Hero 区、统计卡、说明卡和升级说明信息条。
- 顺手修复 `ClipboardHelper` 的过时 API 调用，让构建恢复为 0 warning。

## 修改文件
- `AzrngTools/App.axaml`
- `AzrngTools/AzrngTools.csproj`
- `AzrngTools/Styles/DesignTokens.axaml`
- `AzrngTools/Styles/Global.axaml`
- `AzrngTools/Utils/ClipboardHelper.cs`
- `AzrngTools/Views/MainWindow.axaml`
- `AzrngTools/Views/OverviewPageView.axaml`
- `TASK.md`
- `design-system.yaml`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln -v minimal`
- 结果：通过，0 warning / 0 error
- 未执行：桌面启动 smoke test、人工视觉验收
- 原因：当前回合未接入自动化 UI 启动验证，且终端环境无法直接完成可视化验收

## 风险或遗留项
- 本轮未引入 `Ursa.Avalonia`，因为当前需求更偏主题与信息层次优化，而不是新增复杂导航/对话框组件；后续若要继续升级弹窗、工作台容器或导航组件，再单独引入更稳妥。
- 部分旧页面仍保留原有布局风格，后续可以按高频工具逐页统一到新的工作台卡片体系。


---

## 2026-03-28 DES页布局重叠修复

来源文件：`2026-03-28-DES页布局重叠修复.md`

## 本次目标
- 修复 DES 页面右侧“快捷操作”区域内容重叠

## 核心改动
- 将右侧操作区的 `Grid.RowDefinitions` 从两行恢复为三行
- 保证说明、按钮和提示区分别占据独立行

## 修改文件
- `AzrngTools/Views/Encrypts/DesEncryptPageView.axaml`
- `TASK.md`

## 校验情况
- 已执行：`dotnet build -c Debug -p:UseAppHost=false -p:SkipCopyBuildProduct=true`
- 结果：通过，0 警告，0 错误

## 风险或遗留项
- 本次仅修复单页布局重叠，未扩散调整其它页面


---

## 2026-03-28 DES与SM4页右侧留白优化

来源文件：`2026-03-28-DES与SM4页右侧留白优化.md`

## 本次目标
- 优化 DES 与 SM4 页面右侧区域留白过多的问题

## 核心改动
- 将两页主体从三栏调整为更均衡的两栏结构
- 左侧保留输入区，右侧改为“密钥与结果”加“操作区”的纵向堆叠
- 保留原有命令绑定与字段绑定，不新增任何工具执行逻辑

## 修改文件
- `AzrngTools/Views/Encrypts/DesEncryptPageView.axaml`
- `AzrngTools/Views/Encrypts/Sm4EncryptPageView.axaml`
- `TASK.md`

## 校验情况
- 已执行：`dotnet build AzrngTools/AzrngTools.csproj -c Debug -p:UseAppHost=false -p:SkipCopyBuildProduct=true`
- 结果：通过，0 warning，0 error

## 风险或遗留项
- 本次只调整展示布局，未修改加解密逻辑
- 仍建议在应用内手动查看 DES 与 SM4 页在不同窗口宽度下的实际观感


---

## 2026-03-28 DES与SM4页右侧区域重叠修复

来源文件：`2026-03-28-DES与SM4页右侧区域重叠修复.md`

## 本次目标
- 修复 DES 与 SM4 页面右侧操作区域内容重叠问题

## 核心改动
- 重整 `DesEncryptPageView` 右侧卡片结构，移除冗余说明卡片，仅保留标题与操作按钮
- 修复 `Sm4EncryptPageView` 右侧卡片 `Grid.RowDefinitions` 数量不足的问题，保证格式区与按钮区各占独立行
- 为两个页面右侧卡片补充 `VerticalAlignment="Top"`，避免在多列等高布局下产生挤压

## 修改文件
- `AzrngTools/Views/Encrypts/DesEncryptPageView.axaml`
- `AzrngTools/Views/Encrypts/Sm4EncryptPageView.axaml`
- `TASK.md`

## 校验情况
- 已执行：`dotnet build AzrngTools/AzrngTools.csproj -c Debug -p:UseAppHost=false -p:SkipCopyBuildProduct=true`
- 结果：通过，0 warning，0 error

## 风险或遗留项
- 本次为局部 XAML 布局修复，未额外修改工具执行逻辑
- 仍建议在应用内手动切到 DES 与 SM4 页面看一眼窄宽度下的实际换行效果


---

## 2026-03-28 GUID页左右布局微调

来源文件：`2026-03-28-GUID页左右布局微调.md`

## 本次目标
- 缩窄 GUID 页左侧操作区
- 放宽右侧预览区，尽量让每种 GUID 格式单独一行展示

## 核心改动
- 将 `GuidShowPageView.axaml` 主体布局调整为更窄的左列和更宽的右列
- 将右侧预览区从双列卡片改为单列五行布局
- 将 GUID 预览文本框改为 `TextWrapping="NoWrap"`，优先保证单行显示

## 修改文件
- `TASK.md`
- `AzrngTools/Views/TextHandle/GuidShowPageView.axaml`
- `doc/devlog/2026-03-28-GUID页左右布局微调.md`

## 校验情况
- 已执行：`dotnet build -c Debug`
- 结果：构建成功，但因运行中的 `AzrngTools.exe` 被占用出现复制警告
- 已执行：`dotnet build -c Debug -p:UseAppHost=false`
- 结果：构建成功，0 warning，0 error

## 风险或遗留项
- 本次是 GUID 页局部布局微调，未做额外行为改动


---

## 2026-03-28 HMAC页布局重叠修复

来源文件：`2026-03-28-HMAC页布局重叠修复.md`

## 本次目标
- 修复 HMAC Hash 页面右侧“算法与格式”区域内容重叠

## 核心改动
- 将右侧操作区的 `Grid.RowDefinitions` 从两行恢复为三行
- 保证标题说明、输出格式和算法按钮各自占据独立行

## 修改文件
- `AzrngTools/Views/Encrypts/HmacHashPageView.axaml`
- `TASK.md`

## 校验情况
- 已执行：`dotnet build -c Debug -p:UseAppHost=false -p:SkipCopyBuildProduct=true`
- 结果：通过，2 个警告，0 个错误
- 说明：`AzrngTools.exe` 正在运行，导致 `bin/Debug` 下现有产物无法删除，但不影响本次编译验证通过

## 风险或遗留项
- 本次仅修复单页布局重叠，未扩散调整其它页面
- 已顺手清理旧的 `obj/Debug/net9.0-windows` 构建产物与本次验证生成的 `obj_verify` 临时目录


---

## 2026-03-28 侧边栏常用入口动态化

来源文件：`2026-03-28-侧边栏常用入口动态化.md`

## 本次目标
- 移除左侧侧边栏里的筛选标签区域
- 将常用入口改为按使用频率动态计算，默认最多展示 3 个入口
- 将使用记录持久化到用户数据目录，便于后续持续计算常用工具

## 核心改动
- 删除 `MainWindow` 左侧的筛选标签 UI 和对应筛选逻辑，侧边栏改为 `搜索 -> 统计 -> 常用入口 -> 分组列表`
- 将常用入口改为“首页 + 最多 2 个动态常用工具”，总数默认不超过 3 个
- 新增 `IToolUsageStatsService` / `ToolUsageStatsService`，把使用记录保存到用户目录下的 `tool-usage-stats.json`
- 在 `MainWindowViewModel` 中记录工具打开次数，并在每次打开工具后重新刷新常用入口排序
- 建议算法采用 `频率平滑 + 最近使用衰减`：
  `score = log2(UseCount + 1) * 0.75 + exp(-daysSinceLastUse / 7) * 0.25`
- 当历史记录不足时，回退到预设推荐工具：`GUID生成器 / JSON格式化 / URL编码解码`

## 修改文件
- `TASK.md`
- `AzrngTools/Views/MainWindow.axaml`
- `AzrngTools/ViewModels/MainWindowViewModel.cs`
- `AzrngTools/Models/ToolUsageRecord.cs`
- `AzrngTools/Models/ToolUsageStore.cs`
- `AzrngTools/Services/IToolUsageStatsService.cs`
- `AzrngTools/Services/ToolUsageStatsService.cs`
- `doc/devlog/2026-03-28-侧边栏常用入口动态化.md`

## 校验情况
- 已执行：`dotnet build -c Debug -p:UseAppHost=false`
- 结果：构建成功，0 warning，0 error

## 风险或遗留项
- 当前未额外增加“清空使用历史”或“手动固定常用入口”的设置项
- `ToolFilterChip` 模型文件仍保留在仓库中，但已不再被主窗口侧边栏使用


---

## 2026-03-28 加密页与GUID页展示层收口

来源文件：`2026-03-28-加密页与GUID页展示层收口.md`

## 本次目标
- 继续迁移遗留旧页面到统一卡片式展示结构
- 优先处理加密相关工具页与 GUID 生成页
- 不新增工具执行逻辑，只优化页面层次、分区和可读性

## 核心改动
- 将 `EncodePageView` 改造成统一的输入、结果、说明、快捷操作四段式布局
- 将 `Aes / Des / Hash / HmacHash / Sm4 / Rsa` 页面迁移到 `tool-card-roomy + tool-card-soft` 体系，按“输入 / 参数 / 结果 / 操作”重新组织布局
- 保留 RSA 的双标签页结构，但重做两个页签内部的区域划分和信息层级
- 按要求删除 `GuidShowPageView` 底部重复的“完整结果”展示区，仅保留右侧快速预览
- 将 GUID 右侧预览从纯 `TextBlock` 改为只读文本框，方便直接选中复制

## 修改文件
- `TASK.md`
- `AzrngTools/Views/Encode/EncodePageView.axaml`
- `AzrngTools/Views/Encrypts/AesEncryptPageView.axaml`
- `AzrngTools/Views/Encrypts/DesEncryptPageView.axaml`
- `AzrngTools/Views/Encrypts/HashPageView.axaml`
- `AzrngTools/Views/Encrypts/HmacHashPageView.axaml`
- `AzrngTools/Views/Encrypts/RsaEncryptPageView.axaml`
- `AzrngTools/Views/Encrypts/Sm4EncryptPageView.axaml`
- `AzrngTools/Views/TextHandle/GuidShowPageView.axaml`
- `doc/devlog/2026-03-28-加密页与GUID页展示层收口.md`

## 校验情况
- 已执行：`dotnet build -c Debug`
- 结果：构建成功，0 warning，0 error
- 说明：本轮主要是 XAML 展示层调整，已通过编译验证绑定和页面生成；未逐页做桌面端手动点击回归

## 风险或遗留项
- 当前 `T007` 仍未完全结束，`Translator / TodayNews / About / Hardware` 等页面还可以继续收口
- GUID 页这次没有新增复制命令，而是通过只读文本框支持直接选中复制，符合“不新增行为”的约束


---

## 2026-03-28 界面冗余说明清理

来源文件：`2026-03-28-界面冗余说明清理.md`

## 本次目标
- 删除主界面和工具页中不再需要的说明性展示内容
- 收掉面向开发过程的提示文案，保留更直接的用户功能描述

## 核心改动
- 删除右侧内容区顶部页头卡片
- 删除侧边栏统计中的“当前可见”
- 删除 GUID 页底部说明区
- 删除多页中“本次只统一页面结构”“这页只升级布局层级”等说明卡片
- 将部分“这页/这一页”表述改为直接面向用户的功能描述
- 简化首页右侧说明区与统计项，保留更必要的信息

## 修改文件
- `AzrngTools/Views/MainWindow.axaml`
- `AzrngTools/Views/OverviewPageView.axaml`
- `AzrngTools/Views/TextHandle/GuidShowPageView.axaml`
- `AzrngTools/Views/TextHandle/MarkdownPageView.axaml`
- `AzrngTools/Views/TextHandle/PasswordGeneratorPageView.axaml`
- `AzrngTools/Views/Encrypts/AesEncryptPageView.axaml`
- `AzrngTools/Views/Encrypts/DesEncryptPageView.axaml`
- `AzrngTools/Views/Encrypts/HashPageView.axaml`
- `AzrngTools/Views/Encrypts/HmacHashPageView.axaml`
- `AzrngTools/Views/Encrypts/Sm4EncryptPageView.axaml`
- `AzrngTools/Views/Encode/Base64EncodePageView.axaml`
- `AzrngTools/Views/Encode/Base64ToImagePageView.axaml`
- `AzrngTools/Views/Encode/EncodePageView.axaml`
- `AzrngTools/Views/Encode/JwtEncodePageView.axaml`
- `AzrngTools/Views/Encode/UnicodeEncodePageView.axaml`
- `AzrngTools/Views/Format/JsonPageView.axaml`
- `AzrngTools/Views/Format/MimeQueryPageView.axaml`
- `AzrngTools/Views/Format/SqlFormatPageView.axaml`
- `AzrngTools/Views/Format/StringPageView.axaml`
- `AzrngTools/Views/Format/XmlToHtmlPageView.axaml`
- `AzrngTools/Views/Setting/HardwarePageView.axaml`
- `TASK.md`

## 校验情况
- 已执行：`dotnet build -c Debug -p:UseAppHost=false`
- 结果：通过，0 警告，0 错误

## 风险或遗留项
- 本次主要是展示文案和卡片区域清理，已完成编译校验，未逐页做手工视觉回归


---

## 2026-03-28 今日新闻功能下线

来源文件：`2026-03-28-今日新闻功能下线.md`

## 本次目标
- 删除今日新闻功能入口与相关实现

## 核心改动
- 从主窗口菜单中移除“今日新闻”入口
- 删除今日新闻页面、视图模型和对应的视图注册、DI 注册
- 删除仅被今日新闻功能使用的数据模型文件

## 修改文件
- `AzrngTools/ViewModels/MainWindowViewModel.cs`
- `AzrngTools/App.axaml.cs`
- `AzrngTools/ViewModels/Other/TodayNewsPageViewModel.cs`
- `AzrngTools/Views/Other/TodayNewsPageView.axaml`
- `AzrngTools/Views/Other/TodayNewsPageView.axaml.cs`
- `AzrngTools/Models/GetNewsDto.cs`
- `TASK.md`

## 校验情况
- 已执行：`dotnet build -c Debug -p:UseAppHost=false`
- 结果：通过，0 警告，0 错误

## 风险或遗留项
- 本次是功能下线，不涉及其它工具执行逻辑调整


---

## 2026-03-28 旧工具页展示层收口

来源文件：`2026-03-28-旧工具页展示层收口.md`

## 本次目标
- 继续收口遗留工具页的展示层结构，统一到当前全局卡片样式
- 只优化现有工具的页面层次与信息分区，不新增工具执行逻辑
- 按批次提交改动，保证每批都有独立的构建校验

## 核心改动
- 将 `Base64 转图片`、`正则分析`、`人民币大写转换` 三页迁移为统一的页头 + 主内容卡片 + 操作说明布局
- 将 `SQL 整理` 页面改为单编辑区工作台样式，保留原位回写的处理模式，补充操作说明与快捷操作区
- 将 `XML 转 HTML` 页面拆分为 `XML 输入 / XSLT 输入 / 执行转换 / HTML 输出` 四个明确区域
- 将 `Markdown 预览` 页面改为统一卡片式双栏布局，移除旧页面里的硬编码黑色边框
- 所有本次修改页面均优先复用 `tool-card-roomy`、`tool-card-soft`、`section-title`、`section-copy`、`badge-pill`、`tool-actions` 等现有全局样式

## 修改文件
- `TASK.md`
- `AzrngTools/Views/Encode/Base64ToImagePageView.axaml`
- `AzrngTools/Views/Format/RegexAnalysisView.axaml`
- `AzrngTools/Views/Format/RMBConvertPageView.axaml`
- `AzrngTools/Views/Format/SqlFormatPageView.axaml`
- `AzrngTools/Views/Format/XmlToHtmlPageView.axaml`
- `AzrngTools/Views/TextHandle/MarkdownPageView.axaml`
- `doc/devlog/2026-03-28-旧工具页展示层收口.md`

## 校验情况
- 已执行：`dotnet build -c Debug`
- 结果：构建成功，0 warning，0 error
- 说明：本轮主要是 XAML 展示层调整，已通过编译确认页面入口可生成；当前未在桌面端逐页手动点击回归

## 风险或遗留项
- 本次未改动任何 ViewModel 命令和工具执行逻辑，若后续需要继续优化交互反馈，应单独立任务处理
- 目前这批优先页面已完成统一样式收口，其他未触达页面仍可能保留旧版展示结构


---

## 2026-03-28 首页入口与常用入口调整

来源文件：`2026-03-28-首页入口与常用入口调整.md`

## 本次目标
- 删除首页概览页底部已不需要的推荐区块
- 将侧边栏常用入口与首页入口拆分，首页改为独立按钮
- 保证常用入口统计与展示都不再包含首页

## 核心改动
- 在主窗口侧边栏统计卡前增加独立首页图标按钮，点击直接回到首页
- 常用入口列表改为只展示工具页，最多保留 3 个动态入口
- 首页保持默认落点，但不再占用常用入口名额
- 总工具数与当前可见数改为按真实工具项统计，不再把首页算进去
- 删除首页概览页底部“推荐先体验的页面 / 下一步建议”区域

## 修改文件
- `AzrngTools/ViewModels/MainWindowViewModel.cs`
- `AzrngTools/Views/MainWindow.axaml`
- `AzrngTools/Views/OverviewPageView.axaml`
- `TASK.md`

## 校验情况
- 已执行：`dotnet build -c Debug -p:UseAppHost=false`
- 结果：通过，0 警告，0 错误

## 风险或遗留项
- 本次主要是侧边栏与首页概览布局调整，已完成编译校验，未额外做手工视觉回归
- 首页概览页历史文件存在终端显示编码噪声，本次已避免扩大改动范围，后续若统一清理编码可单独处理


---

## 2026-03-28 尾页展示层收尾

来源文件：`2026-03-28-尾页展示层收尾.md`

## 本次目标
- 完成 `Translator / TodayNews / About / Hardware` 四个尾页的展示层统一
- 将遗留旧布局页全部迁移到当前统一卡片样式体系
- 不新增行为，只优化信息分区、层级和可读性

## 核心改动
- 将 `TranslatorPageView` 改造成输入、翻译设置、结果三段式工作台布局
- 将 `TodayNewsPageView` 改造成页头、日期概览、新闻列表分区布局，并保留原有抓取逻辑
- 将 `AboutPageView` 收口到当前页面头图和卡片样式体系，移除旧的硬编码颜色写法
- 将 `HardwarePageView` 改造成硬件信息概览卡片布局，统一只读展示样式
- 校验当前 `Views` 下工具页均已接入 `tool-card-roomy` 或 `tool-card-soft` 统一卡片类

## 修改文件
- `TASK.md`
- `AzrngTools/Views/Other/TranslatorPageView.axaml`
- `AzrngTools/Views/Other/TodayNewsPageView.axaml`
- `AzrngTools/Views/Setting/AboutPageView.axaml`
- `AzrngTools/Views/Setting/HardwarePageView.axaml`
- `doc/devlog/2026-03-28-尾页展示层收尾.md`

## 校验情况
- 已执行：`dotnet build -c Debug`
- 结果：构建成功，0 warning，0 error
- 已执行：扫描 `AzrngTools/Views` 下遗留旧布局页
- 结果：除 `MainWindow` 和 `OverviewPageView` 外，本轮目标页均已接入统一卡片样式

## 风险或遗留项
- 本轮未逐页做桌面端手动点击回归，当前主要依赖编译通过和结构检查
- `T007` 当前已进入 `REVIEW`，若用户确认整体效果无误，可在下一次交付时转为 `DONE`


---

## 2026-03-28 字数统计页统计卡片遮挡修复

来源文件：`2026-03-28-字数统计页统计卡片遮挡修复.md`

## 本次目标
- 修复字数统计页中基础统计和格式统计卡片的标签与数值遮挡问题

## 核心改动
- 为 `WordCountPageView.axaml` 中统计行的数值文本补充 `Grid.Column="1"`
- 让标签继续位于左列、数值位于右列，避免两者同时落在第 0 列发生重叠

## 修改文件
- `AzrngTools/Views/Format/WordCountPageView.axaml`
- `doc/devlog/2026-03-28-字数统计页统计卡片遮挡修复.md`

## 校验情况
- 已执行：`dotnet build -c Debug`
- 结果：构建成功，0 warning，0 error

## 风险或遗留项
- 本次是局部 XAML 布局修复，未做桌面端手动回归截图确认


---

## 2026-03-30 版本信息来源与展示位置调整

来源文件：`2026-03-30-版本信息来源与展示位置调整.md`

## 本次目标
- 将版本号迁移到工程配置统一管理
- 移除首页中的版本信息卡片
- 将版本信息调整到更合适的关于页展示

## 核心改动
- 在 `AzrngTools.csproj` 中增加 `Version`、`FileVersion`、`InformationalVersion`
- 新增 `IAppInfoService` / `AppInfoService`，统一从程序集读取版本信息
- 首页移除版本信息卡片，保留工作台说明与技术栈标签
- 关于页新增版本信息卡片，通过 ViewModel 绑定统一版本来源

## 修改文件
- `AzrngTools/AzrngTools.csproj`
- `AzrngTools/App.axaml.cs`
- `AzrngTools/Services/IAppInfoService.cs`
- `AzrngTools/Services/AppInfoService.cs`
- `AzrngTools/ViewModels/OverviewPageViewModel.cs`
- `AzrngTools/ViewModels/Setting/AboutPageViewModel.cs`
- `AzrngTools/Views/OverviewPageView.axaml`
- `AzrngTools/Views/Setting/AboutPageView.axaml`
- `TASK.md`
- `doc/devlog/2026-03-30-版本信息来源与展示位置调整.md`

## 校验情况
- 已执行：`dotnet build`
- 结果：通过，0 警告，0 错误

## 风险或遗留项
- 当前显示版本优先读取 `InformationalVersion`
- `app.manifest` 中的 `assemblyIdentity version` 仍是独立值，如后续需要和程序集版本完全对齐，可再统一收口
- 尚未在实际运行界面中手动确认首页移除后的留白和关于页版本卡片观感


---

## 2026-03-30 版本展示改为仅显示Version值

来源文件：`2026-03-30-版本展示改为仅显示Version值.md`

## 本次目标
- 修正关于页版本信息展示
- 页面仅显示 `csproj` 中 `<Version>` 对应的版本值，不显示附加哈希

## 核心改动
- 调整 `AppInfoService` 的版本读取逻辑
- 不再读取 `InformationalVersion`
- 改为直接读取程序集版本，避免显示 `+commit hash`

## 修改文件
- `AzrngTools/Services/AppInfoService.cs`
- `TASK.md`
- `doc/devlog/2026-03-30-版本展示改为仅显示Version值.md`

## 校验情况
- 已执行：`dotnet build`
- 结果：通过，0 警告，0 错误

## 风险或遗留项
- 当前版本展示依赖程序集版本生成规则，适用于当前四段纯数字版本号
- 尚未在实际运行界面中手动确认关于页显示值已变为 `2026.3.30.2256`


---

## 2026-03-30 关于页说明清理与头像圆角

来源文件：`2026-03-30-关于页说明清理与头像圆角.md`

## 本次目标
- 删除关于页中两段面向开发过程的说明性文案
- 将关于页头像 `user.jpg` 调整为圆角展示，保持与现有样式资源一致

## 核心改动
- 删除 `AboutPageView` 中“项目印象”和“页面寄语”卡片顶部的说明文案
- 在头像外层容器内增加圆角裁剪容器，使 `user.jpg` 以圆角形式展示
- 同步更新 `TASK.md` 任务状态，记录本次页面微调

## 修改文件
- `AzrngTools/Views/Setting/AboutPageView.axaml`
- `TASK.md`
- `doc/devlog/2026-03-30-关于页说明清理与头像圆角.md`

## 校验情况
- 已执行：`dotnet build`
- 结果：通过，0 警告，0 错误

## 风险或遗留项
- 本次为 About 页面展示层微调，未改动 ViewModel 和业务逻辑
- 已完成构建校验，尚未在实际运行界面中手动复核头像圆角与页面间距表现


---

## 2026-03-30 主窗口右上角寄语补充

来源文件：`2026-03-30-主窗口右上角寄语补充.md`

## 本次目标
- 在主窗口右上角增加固定寄语文案
- 保持标题栏现有排版和样式体系不被破坏

## 核心改动
- 在主窗口标题栏右侧操作区前追加一段寄语文本
- 复用现有 `app-subtitle` 文本样式，避免新增硬编码字号和颜色

## 修改文件
- `AzrngTools/Views/MainWindow.axaml`
- `TASK.md`
- `doc/devlog/2026-03-30-主窗口右上角寄语补充.md`

## 校验情况
- 已执行：`dotnet build`
- 结果：通过，0 警告，0 错误

## 风险或遗留项
- 本次仅调整标题栏展示层，未改动业务逻辑
- 已完成构建校验，尚未在实际运行窗口中手动确认右上角文本与按钮间距、截断表现


---

## 2026-03-30 左侧目录展开状态修复

来源文件：`2026-03-30-左侧目录展开状态修复.md`

## 本次目标
- 让左侧分组目录默认收起
- 修复展开某个分组后其他分组跟着自动展开的问题
- 调整为单个分组展开时其余分组自动收起的手风琴行为

## 核心改动
- 为 `MenuBar` 增加 `IsExpanded` 状态，用于承接分组展开绑定
- 将侧栏 `Expander` 从固定展开改为双向绑定分组状态
- 在 `MainWindowViewModel` 中记录并恢复各分组展开状态
- 为分组注册属性变更监听，当前分组展开时自动折叠其他分组

## 修改文件
- `AzrngTools/Models/MenuBar.cs`
- `AzrngTools/ViewModels/MainWindowViewModel.cs`
- `AzrngTools/Views/MainWindow.axaml`
- `TASK.md`
- `doc/devlog/2026-03-30-左侧目录展开状态修复.md`

## 校验情况
- 已执行：`dotnet build`
- 结果：通过，0 警告，0 错误

## 风险或遗留项
- 已完成编译校验，尚未在实际运行界面中手动逐项验证各分组展开/收起交互
- 搜索状态下当前实现仍会默认展开匹配分组，便于直接看到结果


---

## 2026-03-31 DbTools 融合迁移方案文档

来源文件：`2026-03-31-DbTools融合迁移方案文档.md`

## 本次目标
- 输出 `DbTools` 融合 `AzrngTool` 的迁移清单与信息架构方案
- 明确采用 `方案 B` 作为开发辅助区的组织方式

## 核心改动
- 新增迁移方案文档，明确 `DbTools` 为主产品、`AzrngTool` 为辅助能力来源
- 在文档中划分第一批、第二批、暂缓迁移能力
- 明确一级导航、二级结构与页面组织方式
- 明确技术迁移边界与实施阶段建议

## 修改文件
- `doc/DbTools融合AzrngTool迁移方案.md`
- `TASK.md`
- `doc/devlog/2026-03-31-DbTools融合迁移方案文档.md`

## 校验情况
- 本次仅涉及文档与任务记录更新，未执行 `dotnet build`

## 风险或遗留项
- 当前方案为产品与结构层建议，尚未开始实际代码迁移
- 具体迁移时仍需再按 `DbTools` 现有代码结构细化到页面和模块级别


---

## 2026-03-31 GitHub 自动更新链路

来源文件：`2026-03-31-GitHub自动更新链路.md`

## 本次目标
- 通过 GitHub Actions 生成可下载的 Windows 发布 zip
- 在关于页增加检查更新与自动更新入口
- 让客户端可以基于 GitHub Release 完成版本检查与自动替换

## 核心改动
- 新增 `IAppUpdateService` / `AppUpdateService`，调用 GitHub Releases API 获取最新版本与 zip 下载地址
- 新增更新包下载、解压、生成本地 PowerShell 更新脚本的流程，应用退出后自动覆盖当前目录并重启
- 扩展 `IAppInfoService` / `AppInfoService`，统一提供仓库地址、更新包名、安装目录与当前可执行文件路径
- 新增 `IApplicationRuntimeService` / `ApplicationRuntimeService`，通过服务抽象关闭应用，避免在 ViewModel 中直接处理窗口生命周期
- 改造关于页 `AboutPageViewModel` 与 `AboutPageView`，展示当前版本、最新版本、发布时间、更新状态与发布说明，并增加“检查更新 / 立即更新”按钮
- 新增 `.github/workflows/release.yml`，在推送 `v*` tag 时自动执行 `dotnet publish`、压缩 `AzrngTools-win-x64-portable.zip`、上传 artifact 并发布 GitHub Release
- 更新 `AzrngTools/readme.md`，补充 GitHub Actions 发布流程与关于页自动更新说明

## 修改文件
- `.github/workflows/release.yml`
- `AzrngTools/Models/AppUpdateInfo.cs`
- `AzrngTools/Models/AppUpdateApplyResult.cs`
- `AzrngTools/Services/IAppInfoService.cs`
- `AzrngTools/Services/AppInfoService.cs`
- `AzrngTools/Services/IAppUpdateService.cs`
- `AzrngTools/Services/AppUpdateService.cs`
- `AzrngTools/Services/IApplicationRuntimeService.cs`
- `AzrngTools/Services/ApplicationRuntimeService.cs`
- `AzrngTools/ViewModels/Setting/AboutPageViewModel.cs`
- `AzrngTools/Views/Setting/AboutPageView.axaml`
- `AzrngTools/readme.md`
- `TASK.md`
- `doc/devlog/2026-03-31-GitHub自动更新链路.md`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln -v minimal`
- 结果：通过，0 warning / 0 error
- 未执行：真实 GitHub Release 下载与本地覆盖更新的端到端实机验证
- 原因：当前终端环境未实际运行桌面程序，也未在本次变更中触发真实 tag 发布流程

## 风险或遗留项
- 首次使用检查更新前，需要仓库已经存在至少一个 GitHub Release；否则客户端会收到 GitHub API 的失败提示
- 自动更新依赖当前安装目录具备写权限；如果程序安装在受保护目录，自动覆盖可能失败，此时需要手动下载 release zip
- 当前更新流程采用“覆盖当前目录”的便携版策略，不会清理历史版本遗留文件；若未来发布结构变化较大，可再补更严格的镜像同步或独立 updater


---

## 2026-03-31 main 版本变更自动发布

来源文件：`2026-03-31-main版本变更自动发布.md`

## 本次目标
- 将发布流程改为仅监听 `main` 分支
- 在检测到项目 `Version` 发生变化时自动创建 tag 并发布 Release

## 核心改动
- 更新 `.github/workflows/release.yml`
- 将触发条件从 `v*` tag push 改为 `main` 分支 push
- 在工作流中读取 `AzrngTools/AzrngTools.csproj` 的 `Version`
- 对比本次 push 前后的 `Version`，仅在版本变化时继续发布
- 自动创建并推送 `v版本号` tag
- 继续执行 `dotnet publish`、zip 打包、artifact 上传和 GitHub Release 创建
- 更新根目录 `README.md`，说明新的自动发版流程与版本号规则

## 修改文件
- `.github/workflows/release.yml`
- `README.md`
- `TASK.md`
- `doc/devlog/2026-03-31-main版本变更自动发布.md`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln -v minimal`
- 结果：通过，0 warning / 0 error
- 未执行：真实 main push 后的自动 tag 与 Release 验证
- 原因：本次未向远端仓库推送正式版本提交

## 风险或遗留项
- 当前发版规则要求只在准备正式发布时修改 `Version`，否则推送到 `main` 后会自动发版
- 若同一个版本号已在其他提交上生成 tag，工作流会直接失败并提示先提升版本号


---

## 2026-03-31 补充发布所需工程文件

来源文件：`2026-03-31-补充发布所需工程文件.md`

## 本次目标
- 修复 GitHub Actions Release 发布阶段缺少工程级配置文件导致的失败

## 核心改动
- 确认 `AzrngTools/Roots.xml` 与 `AzrngTools/FodyWeavers.xml` 本地存在但未被 Git 跟踪
- 将上述文件纳入版本控制，确保 CI Runner 能获取到 Release 所需文件
- 在 `.gitignore` 末尾增加白名单规则，避免后续再次被全局 `*.xml` 忽略

## 修改文件
- `AzrngTools/Roots.xml`
- `AzrngTools/FodyWeavers.xml`
- `.gitignore`
- `TASK.md`
- `doc/devlog/2026-03-31-补充发布所需工程文件.md`

## 校验情况
- 已执行：`dotnet publish AzrngTools\AzrngTools.csproj -c Release -r win-x64`
- 结果：通过，成功生成 `publish` 目录产物

## 风险或遗留项
- 当前主要修复的是 CI 缺失文件问题；若远端继续失败，需要再看 AOT 或裁剪链路是否还有其它环境差异


---

## 2026-03-31 发布工作流版本解析修复

来源文件：`2026-03-31-发布工作流版本解析修复.md`

## 本次目标
- 修复 GitHub Actions 中项目版本读取步骤的稳定性问题

## 核心改动
- 更新 `.github/workflows/release.yml`
- 在版本解析步骤中改用绝对路径定位项目文件
- 增加 `Test-Path` 校验，项目文件不存在时给出明确错误
- 将 `Get-Content` 改为 `-Raw -Encoding UTF8` 读取，避免 XML 解析受逐行内容影响
- 提取统一的 XML 版本读取函数，同时复用到当前提交和前一提交版本对比逻辑

## 修改文件
- `.github/workflows/release.yml`
- `TASK.md`
- `doc/devlog/2026-03-31-发布工作流版本解析修复.md`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln -v minimal`
- 结果：通过，0 warning / 0 error
- 未执行：GitHub Actions 远端实跑校验
- 原因：本次仅在本地修复并准备再次推送验证

## 风险或遗留项
- 当前已增强元数据解析的健壮性，但最终仍需一次真实的 `main` 推送来验证远端 runner 行为


---

## 2026-03-31 发布工作流改为正则提取版本

来源文件：`2026-03-31-发布工作流改为正则提取版本.md`

## 本次目标
- 修复 GitHub Actions 在 runner 上解析项目版本时的 XML 兼容性问题

## 核心改动
- 更新 `.github/workflows/release.yml`
- 移除 `LoadXml` 解析方案
- 改为从 `csproj` 文本中使用正则提取 `<Version>...</Version>`
- 同时将历史提交中的项目版本读取逻辑也改为同一套文本提取方法

## 修改文件
- `.github/workflows/release.yml`
- `TASK.md`
- `doc/devlog/2026-03-31-发布工作流改为正则提取版本.md`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln -v minimal`
- 结果：通过，0 warning / 0 error
- 未执行：GitHub Actions 远端实跑校验
- 原因：需要重新推送 `main` 触发 workflow

## 风险或遗留项
- 该方案依赖 `csproj` 中存在明确的 `<Version>...</Version>` 节点；如果以后改成从其他 props 文件导入版本，需要同步调整工作流提取逻辑


---

## 2026-03-31 发布工作流规避 git 非零退出

来源文件：`2026-03-31-发布工作流规避git非零退出.md`

## 本次目标
- 修复 GitHub Actions 中版本元数据步骤因 `git` 查询未命中而导致的非零退出问题

## 核心改动
- 更新 `.github/workflows/release.yml`
- 将历史版本文件存在性判断从 `git cat-file -e` 改为 `git ls-tree`
- 将 tag 存在性判断从 `git rev-parse -q --verify` 改为 `git tag --list`
- 保留现有逻辑，但避免在“查不到是正常情况”时让 step 以 exit code 1 失败

## 修改文件
- `.github/workflows/release.yml`
- `TASK.md`
- `doc/devlog/2026-03-31-发布工作流规避git非零退出.md`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln -v minimal`
- 结果：通过，0 warning / 0 error
- 未执行：GitHub Actions 远端实跑校验
- 原因：需要重新推送 `main` 触发 workflow

## 风险或遗留项
- 当前主要修复的是“版本文件第一次引入时”和“tag 尚不存在时”的正常未命中场景
- 仍建议在远端再跑一次真实 workflow，确认 runner 行为已稳定


---

## 2026-03-31 发布规则与主分支限制补充

来源文件：`2026-03-31-发布规则与主分支限制补充.md`

## 本次目标
- 为自动更新链路补充清晰的版本号约定
- 将 GitHub Release 发布限制为仅允许 `main` 分支上的提交触发

## 核心改动
- 更新 `.github/workflows/release.yml`
- 新增 tag 所属分支校验，只有 tag 对应提交包含在 `origin/main` 中时才允许继续发布
- 在 `AzrngTools/readme.md` 中补充版本号规则，约定使用 `YYYY.M.D.N` 的四段数字格式
- 在文档中明确发布步骤：先合并到 `main`，再打与项目版本一致的 `v` 前缀 tag

## 修改文件
- `.github/workflows/release.yml`
- `AzrngTools/readme.md`
- `TASK.md`
- `doc/devlog/2026-03-31-发布规则与主分支限制补充.md`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln -v minimal`
- 结果：通过，0 warning / 0 error
- 未执行：真实 GitHub Actions tag 发布验证
- 原因：本次未推送实际 tag 到远端仓库

## 风险或遗留项
- 当前工作流仍然由 `v*` tag 触发，但会在流程内校验该 tag 是否指向 `main` 上的提交，不满足条件会直接失败
- 如果未来需要完全改成“push main 自动发版”模式，可以再单独调整为按 `main` push + 版本变更检测的工作流


---

## 2026-03-31 根目录版本文件收口

来源文件：`2026-03-31-根目录版本文件收口.md`

## 本次目标
- 将项目版本号收口到仓库根目录单一文件
- 让工程构建和 GitHub Actions 都从同一份版本来源读取

## 核心改动
- 新增根目录 `VERSION` 文件，作为版本号单一事实来源
- 更新 `AzrngTools/AzrngTools.csproj`，改为从根目录 `VERSION` 读取 `Version`
- 更新 `.github/workflows/release.yml`，改为读取根目录 `VERSION` 并比较前后版本
- 更新 `README.md`，将发版入口从修改 `csproj` 改为修改 `VERSION`

## 修改文件
- `VERSION`
- `AzrngTools/AzrngTools.csproj`
- `.github/workflows/release.yml`
- `README.md`
- `TASK.md`
- `doc/devlog/2026-03-31-根目录版本文件收口.md`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln -v minimal`
- 结果：通过，0 warning / 0 error
- 未执行：GitHub Actions 远端实跑校验
- 原因：需要推送到远端后触发 workflow

## 风险或遗留项
- 以后正式发版只应修改根目录 `VERSION`，不要再手改 `csproj` 中的版本
- 如果未来引入多项目方案，需要评估是否继续共用这一份根目录版本文件


---

## 2026-03-31 首发版本自动发布补充

来源文件：`2026-03-31-首发版本自动发布补充.md`

## 本次目标
- 修复首次发布场景下，版本未变化但仓库尚无对应 tag 时被错误跳过的问题

## 核心改动
- 重写 `.github/workflows/release.yml`
- 在元数据判断阶段新增“首发发布”逻辑
- 当当前 `VERSION` 对应的 tag 不存在时，即使版本未变化，也允许继续创建 tag 并发布
- 统一整理历史兼容性修补逻辑，降低 workflow 脚本继续碎片化演进的风险

## 修改文件
- `.github/workflows/release.yml`
- `TASK.md`
- `doc/devlog/2026-03-31-首发版本自动发布补充.md`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln -v minimal`
- 结果：通过，0 warning / 0 error
- 未执行：GitHub Actions 远端实跑校验
- 原因：需要重新推送 `main` 触发 workflow

## 风险或遗留项
- 当前首发判定基于“是否存在对应 tag”，尚未额外检查 GitHub Release 是否存在
- 如果后续遇到“tag 已创建但 release 创建失败”的中断场景，可再补一层 release 存在性校验


---

## 2026-04-01 main 提交自动 tag 并发布 release

来源文件：`2026-04-01-main提交自动tag并发布release.md`

## 本次目标
- 参考 `DbTools` 的工作流模式，实现 `main` 分支每次提交都自动打 tag 并发布 GitHub Release

## 核心改动
- 重写 `.github/workflows/release.yml`
- 改为在单次 `main` push 工作流中直接生成唯一 tag、打包 zip、上传 artifact 并创建 GitHub Release
- 不再依赖“先创建 tag，再等待 tag 触发第二次工作流”的链路
- 发布 tag 改为带时间戳和短 SHA 的唯一构建标识
- 发布时显式写入 `InformationalVersion=版本号+短SHA`
- 更新 `AppInfoService` 与 `AppUpdateService`，支持识别带构建标识的 release，并在相同版本号下比较构建 ID

## 修改文件
- `.github/workflows/release.yml`
- `AzrngTools/Services/IAppInfoService.cs`
- `AzrngTools/Services/AppInfoService.cs`
- `AzrngTools/Services/AppUpdateService.cs`
- `TASK.md`
- `doc/devlog/2026-04-01-main提交自动tag并发布release.md`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln -v minimal`
- 结果：通过，0 warning / 0 error
- 未执行：GitHub Actions 远端自动 tag / release 校验
- 原因：需要推送到 `main` 后在 GitHub Actions 中确认 Release 页面结果

## 风险或遗留项
- 当前客户端仍以 GitHub Release latest 为更新源，因此是否能检查到更新仍取决于远端 Release 是否成功创建
- 若后续需要区分“正式版本”和“每次 main 构建版”，可能还需要再引入 prerelease 或发布频道概念


---

## 2026-04-01 Release 发布与 404 提示补充

来源文件：`2026-04-01-Release发布与404提示补充.md`

## 本次目标
- 对齐 `DbTools` 的 Release 创建经验，补强当前仓库的发布与更新提示链路

## 核心改动
- 更新 `.github/workflows/release.yml`
- 在 `softprops/action-gh-release` 步骤中显式传入 `GITHUB_TOKEN`
- 增加 `make_latest: true`，确保新建 Release 会作为 latest 发布
- 更新 `AzrngTools/Services/AppUpdateService.cs`
- 当 `releases/latest` 返回 404 时，改为抛出明确的用户可读提示，说明当前仓库尚未有正式 Release

## 修改文件
- `.github/workflows/release.yml`
- `AzrngTools/Services/AppUpdateService.cs`
- `TASK.md`
- `doc/devlog/2026-04-01-Release发布与404提示补充.md`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln -v minimal`
- 结果：通过，0 warning / 0 error
- 未执行：GitHub Actions 远端 Release 创建校验
- 原因：需要重新推送 `main` 或手动运行 workflow 后确认仓库中已生成 Release

## 风险或遗留项
- 若远端仍然没有生成 Release，需要检查 GitHub Actions 运行记录中 `Publish GitHub release` 步骤是否成功
- 当前客户端检查更新依赖公开可访问的 GitHub Release；若仓库未来改为私有，需要额外设计认证策略


---

## 2026-04-01 Tag 触发 Release 补发修复

来源文件：`2026-04-01-Tag触发Release补发修复.md`

## 本次目标
- 修复“仓库已有版本 tag，但 Releases 页面仍为空”时无法自动补发 GitHub Release 的问题

## 核心改动
- 更新 `.github/workflows/release.yml`
- 将发布流程拆分为两段：
  - `main` 推送时负责检测版本并补建 `v版本号` tag
  - `v*` tag 推送或手动触发时负责执行发布、打包与创建 GitHub Release
- 增加 `workflow_dispatch`，支持在 Actions 中选择已有 tag 手动补发 Release
- 更新 `README.md`，补充 tag 与 release 的触发关系，以及已有 tag 的补发方式

## 修改文件
- `.github/workflows/release.yml`
- `README.md`
- `TASK.md`
- `doc/devlog/2026-04-01-Tag触发Release补发修复.md`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln -v minimal`
- 结果：通过，0 warning / 0 error
- 已执行：`git diff --check`
- 结果：未发现 diff 格式错误，仅提示仓库工作区将在后续由 Git 按设置处理 LF/CRLF
- 未执行：GitHub Actions 远端 tag / workflow_dispatch 实跑校验
- 原因：需要推送到 GitHub 后由远端环境触发

## 风险或遗留项
- 当前补发依赖在 Actions 页面手动选择正确的 tag 运行工作流
- GitHub Release 的最终创建结果仍需以远端 Actions 运行日志为准


---

## 2026-04-01 发布包移除 pdb 文件

来源文件：`2026-04-01-发布包移除pdb文件.md`

## 本次目标
- 在 GitHub Actions 打包发布前移除 `AzrngTools.pdb`

## 核心改动
- 更新 `.github/workflows/release.yml`
- 在压缩 `publish/win-x64` 目录前，先删除 `AzrngTools.pdb`
- 保证最终 release zip 中不再包含调试符号文件

## 修改文件
- `.github/workflows/release.yml`
- `TASK.md`
- `doc/devlog/2026-04-01-发布包移除pdb文件.md`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln -v minimal`
- 结果：通过，0 warning / 0 error
- 未执行：GitHub Actions 远端打包产物检查
- 原因：需要推送到 `main` 后检查 release zip 实际内容

## 风险或遗留项
- 当前仅移除 `AzrngTools.pdb`，其他符号或诊断文件仍按发布输出默认行为保留


---

## 2026-04-01 更新检查 SSL 兼容修复

来源文件：`2026-04-01-更新检查SSL兼容修复.md`

## 本次目标
- 修复关于页检查更新时与 GitHub 建立 SSL/TLS 连接失败的问题

## 核心改动
- 更新 `AzrngTools/App.axaml.cs`
- 为更新服务注册命名 `HttpClient`
- 将默认请求版本固定为 HTTP/1.1，并启用常见压缩格式解压
- 重写 `AzrngTools/Services/AppUpdateService.cs`
- 为更新检查和更新包下载增加兼容模式重试
- 首次请求失败时，自动回退到更保守的 `HttpClientHandler` 配置重试
- 在兼容模式中显式启用 TLS 1.2 / TLS 1.3，并关闭证书吊销检查以提升部分环境下的可连通性
- 增加日志记录与更明确的用户提示，避免仅出现底层 SSL 英文异常

## 修改文件
- `AzrngTools/App.axaml.cs`
- `AzrngTools/Services/AppUpdateService.cs`
- `TASK.md`
- `doc/devlog/2026-04-01-更新检查SSL兼容修复.md`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln -v minimal`
- 结果：通过，0 warning / 0 error
- 未执行：真实 Release 包环境下的 GUI 检查更新验证
- 原因：需要重新发布并在桌面界面中手动点击“检查更新”确认

## 风险或遗留项
- 当前兼容模式关闭了证书吊销检查，优先目标是恢复 GitHub 连接稳定性
- 如果后续需要进一步收紧安全策略，可以在确认问题根因后再改为更严格的网络配置


---

## 2026-04-01 关于页更新区右移合并

来源文件：`2026-04-01-关于页更新区右移合并.md`

## 本次目标
- 将关于页中的“更新中心”和“更新提示”合并展示到右侧区域，减少信息分散。

## 核心改动
- 调整 `AboutPageView` 两栏布局，左侧仅保留项目信息。
- 将更新版本信息、检查状态、更新按钮与更新提示统一收拢到右侧“更新中心”卡片中。
- 保持现有样式资源与按钮交互绑定不变，只做展示结构整理。

## 修改文件
- `AzrngTools/Views/Setting/AboutPageView.axaml`
- `TASK.md`
- `doc/devlog/2026-04-01-关于页更新区右移合并.md`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln -v minimal`
- 结果：通过，0 警告，0 错误

## 风险或遗留项
- 本次仅涉及关于页布局调整，未改动自动更新服务与 GitHub Release 检查逻辑。


---

## 2026-04-01 关于页移除发布说明

来源文件：`2026-04-01-关于页移除发布说明.md`

## 本次目标
- 移除关于页右侧展示的发布说明文本区域，保留检查更新与自动更新提示能力。

## 核心改动
- 删除 `AboutPageView` 中右侧“发布说明”文本框，只保留更新提示卡片。
- 清理 `AboutPageViewModel` 中不再使用的 `ReleaseNotes` 展示属性与赋值逻辑。
- 顺手整理关于页与更新状态相关提示文案，避免继续显示乱码文本。

## 修改文件
- `AzrngTools/Views/Setting/AboutPageView.axaml`
- `AzrngTools/ViewModels/Setting/AboutPageViewModel.cs`
- `TASK.md`
- `doc/devlog/2026-04-01-关于页移除发布说明.md`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln -v minimal`
- 结果：通过，0 警告，0 错误

## 风险或遗留项
- 本次仅调整关于页展示与对应 ViewModel 展示字段，未改动更新下载与 Release 拉取逻辑。
- 已完成构建校验，但未在实际 UI 中手动点按“检查更新”复核最终视觉效果。


---

## 2026-04-03 DbTools 数据库工作台并入

来源文件：`2026-04-03-DbTools数据库工作台并入.md`

## 本次目标
- 将 `D:\GitHub\DbTools` 的核心数据库能力并入当前仓库，作为 `AzrngTools` 中的一个数据库大模块接入现有导航。

## 核心改动
- 将 `DbTools` 的 `SmartSQL.UI` 子项目完整复制到当前仓库 `Modules/SmartSQL.UI`，保留其连接管理、数据库树浏览、对象详情与文档导出能力。
- 在主应用中新增“数据库工具 -> 数据库工作台”入口，通过 `DatabaseWorkbenchPageView` 作为适配页承载 `SmartSQL.UI` 的主工作台视图模型与子视图。
- 为当前应用补充 `SmartSQL.UI` 的样式资源、转换器和项目引用，使数据库模块可以在主壳内直接运行。
- 调整 `ViewLocator` 与主窗口导航页承载逻辑，使当前应用能够渲染非本地 `ViewModelBase` 继承链的已注册页面。

## 修改文件
- `AzrngTools.sln`
- `AzrngTools/AzrngTools.csproj`
- `AzrngTools/App.axaml`
- `AzrngTools/App.axaml.cs`
- `AzrngTools/ViewLocator.cs`
- `AzrngTools/ViewModels/MainWindowViewModel.cs`
- `AzrngTools/Views/Database/DatabaseWorkbenchPageView.axaml`
- `AzrngTools/Views/Database/DatabaseWorkbenchPageView.axaml.cs`
- `Modules/SmartSQL.UI/*`
- `README.md`
- `TASK.md`
- `doc/devlog/2026-04-03-DbTools数据库工作台并入.md`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln`
- 结果：通过，0 警告，0 错误

## 风险或遗留项
- 当前数据库模块以子项目方式并入，样式资源仍主要沿用 `SmartSQL.UI` 现有设计语言，后续如果要进一步统一视觉，需要继续做资源映射与页面收口。
- 本次完成了编译级验证，但未在本地逐项手动验证数据库连接、对象浏览、导出与备注编辑等交互链路。


---

## 2026-04-03 GUID 页生成按钮遮挡修复

来源文件：`2026-04-03-GUID页生成按钮遮挡修复.md`

## 本次目标
- 修复 GUID 页面左侧“生成操作”区域中生成按钮被下方说明卡片遮挡的问题

## 核心改动
- 将 `GuidShowPageView.axaml` 左侧操作卡内部 `Grid` 的行定义从两行调整为三行
- 让标题说明、生成按钮、格式说明卡片恢复为正常的纵向排布，避免按钮与说明卡片落在同一行层级

## 修改文件
- `TASK.md`
- `AzrngTools/Views/TextHandle/GuidShowPageView.axaml`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln`
- 结果：通过，0 warning，0 error

## 风险或遗留项
- 本轮只修复了 GUID 页面已确认的遮挡问题，没有继续调整其它文本工具页的视觉细节
- 从当前扫描结果看，`JsonSchemaPageView`、`JsonToCsharpPageView`、`PasswordGeneratorPageView` 不存在同一类少行定义导致的按钮遮挡问题


---

## 2026-04-03 表详情重复名称标签移除

来源文件：`2026-04-03-表详情重复名称标签移除.md`

## 本次目标
- 删除数据表详情区中与顶部标题重复的表名标签，避免右侧工作区重复展示同一对象名称

## 核心改动
- 在 `TableDetailContent.axaml` 中移除“数据表详情”标题区右上角重复展示 `SelectedTable.Name` 的状态标签
- 保留顶部工具栏中的表名与备注展示，避免影响当前工作区上下文识别

## 修改文件
- `TASK.md`
- `AzrngTools/Views/Database/Workbench/TableDetailContent.axaml`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln`
- 结果：通过，0 warning，0 error

## 风险或遗留项
- 本次只删除表详情页中的重复标签，未改动视图详情页和存储过程详情页；这两处当前显示的是功能语义标签，不是对象名重复


---

## 2026-04-03 对象浏览器滚动隔离修复

来源文件：`2026-04-03-对象浏览器滚动隔离修复.md`

## 本次目标
- 让数据库工作台左侧对象浏览器在内容超出时只滚动自身，不再把滚轮事件继续传递给右侧详情工作区

## 核心改动
- 在 `DatabaseWorkbenchPageView.axaml` 的对象树 `ScrollViewer` 上关闭 `IsScrollChainingEnabled`
- 补充显式的横向滚动关闭配置，确保左侧对象浏览器优先承担自身纵向滚动

## 修改文件
- `TASK.md`
- `AzrngTools/Views/Database/DatabaseWorkbenchPageView.axaml`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln`
- 结果：通过，0 warning，0 error

## 风险或遗留项
- 本次针对的是主工作台左侧对象树与右侧详情区之间的滚轮串联
- 如果后续发现表详情、视图详情、存储过程详情内部还有其他嵌套滚动串联，再单独做第二轮细化


---

## 2026-04-03 对象详情页头部信息卡删除

来源文件：`2026-04-03-对象详情页头部信息卡删除.md`

## 本次目标
- 删除表、视图、存储过程详情页顶部独立的信息卡区域
- 将对象备注直接并到对象名称后面展示，减少头部重复信息

## 核心改动
- 在 `TableDetailContent.axaml` 中移除备注、行数、创建时间、更新时间四张头部信息卡
- 在 `ViewDetailView.axaml` 和 `StoredProcedureDetailView.axaml` 中移除备注、创建时间、更新时间三张头部信息卡
- 将表、视图、存储过程的备注改为直接跟在对象名称后，以 `· 备注内容` 的形式展示
- 同步收紧空态说明文案，去掉已删除的时间/规模信息描述

## 修改文件
- `TASK.md`
- `AzrngTools/Views/Database/Workbench/TableDetailContent.axaml`
- `AzrngTools/Views/Database/Workbench/ViewDetailView.axaml`
- `AzrngTools/Views/Database/Workbench/StoredProcedureDetailView.axaml`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln`
- 结果：通过，0 warning，0 error

## 风险或遗留项
- 当前备注直接跟在标题后时，若备注很长，标题区域横向空间会被占用；后续如遇到超长备注，可再收口为截断展示加悬浮提示
- 本轮只处理了对象详情页头部信息区，字段表格、视图列表和存储过程列表中的备注展示方式未调整


---

## 2026-04-03 连接编辑区品牌头主题收口

来源文件：`2026-04-03-连接编辑区品牌头主题收口.md`

## 本次目标
- 去掉连接编辑区头部对品牌色字符串的依赖
- 让连接配置编辑区头部徽标回到统一的工作台主题语言

## 核心改动
- 将 `ConnectionDialog.axaml` 中编辑区头部右侧徽标从品牌色背景/边框改为统一的 `connectionDialogBadge` 主题样式
- 为品牌简称和字标补充统一文本样式，保留数据库识别信息，但不再依赖品牌色差异
- 删除 `ConnectionDialogViewModel.cs` 中仅服务于旧头部徽标的 `CurrentBrandAccentColor`、`CurrentBrandSoftColor`、`CurrentBrandBorderColor` 属性及其通知

## 修改文件
- `TASK.md`
- `AzrngTools/Views/Database/Workbench/ConnectionDialog.axaml`
- `AzrngTools/ViewModels/Database/ConnectionDialogViewModel.cs`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln`
- 结果：通过，0 warning，0 error

## 风险或遗留项
- 本次保留了 `CurrentBrandMonogram` 和 `CurrentBrandWordmark`，仅移除了品牌色差异
- 如果后续还想进一步统一，已保存连接列表里的数据库类型图标底色也可以继续收口


---

## 2026-04-03 连接弹窗类型卡片主题收口

来源文件：`2026-04-03-连接弹窗类型卡片主题收口.md`

## 本次目标
- 将连接管理弹窗中的数据库类型卡片切回当前数据库工作台主题语义
- 移除卡片视觉对 ViewModel 品牌色字符串的依赖

## 核心改动
- 在 `ConnectionDialog.axaml` 中为数据库类型卡片补充统一的 surface、badge、state 和 hover/disabled 样式
- 将卡片内部的边框、顶部色条、徽标和状态标签全部改为基于主题 token 的表现，不再直接绑定品牌色字段
- 精简 `ConnectionDialogViewModel.cs` 中的 `DatabaseTypeCard` 模型，移除仅服务于旧卡片样式的颜色字段
- 统一卡片状态文案为“可配置 / 待接入”，与独立 `DatabaseTypeSelector` 保持一致

## 修改文件
- `TASK.md`
- `AzrngTools/Views/Database/Workbench/ConnectionDialog.axaml`
- `AzrngTools/ViewModels/Database/ConnectionDialogViewModel.cs`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln`
- 结果：通过，0 warning，0 error

## 风险或遗留项
- 本次只收口连接弹窗中的数据库类型卡片，不改动编辑区头部仍在使用的 `CurrentBrand*` 颜色数据
- 如果后续继续追求完全去品牌色字符串化，还可以再处理编辑区顶部品牌头和已保存连接列表的图标底色逻辑


---

## 2026-04-03 连接管理链路主题收口

来源文件：`2026-04-03-连接管理链路主题收口.md`

## 本次目标
- 将连接管理窗口切回当前数据库工作台主题语言
- 收口连接配置表单中的旧灰底提示块、旧错误块和硬编码必填标记

## 核心改动
- 在 `ConnectionDialog.axaml` 中新增一组本地样式，用于统一窗口外壳、分区边界、列表卡片、提示块、错误块与标题文本
- 将连接管理左侧列表、数据库类型选择、右侧编辑器和底部按钮区的白底浅蓝边框改为 `Brush.Panel.*` / `Brush.Accent.*` 体系
- 为 `connectionAction` 增加 `primary` / `secondary` 两种按钮语义，替换连接管理页中重复的内联按钮配色
- 在 `ConnectionConfigForm.axaml` 中新增必填星号、提示块、错误块的局部样式，去掉旧硬编码灰底和红底

## 修改文件
- `TASK.md`
- `AzrngTools/Views/Database/Workbench/ConnectionDialog.axaml`
- `AzrngTools/Views/Database/Workbench/ConnectionConfigForm.axaml`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln`
- 结果：通过，0 warning，0 error

## 风险或遗留项
- `ConnectionDialog.axaml` 内仍保留部分品牌色绑定和少量旧内联尺寸，当前重点是先完成主题语言统一，后续可继续把数据库类型卡片和编辑器内容区再细化
- `DatabaseTypeSelector.axaml` 这个独立控件文件仍有旧硬编码色值，如果后续要继续统一数据库模块视觉，可作为下一轮优化对象


---

## 2026-04-03-数据表详情工作区布局继续融合

来源文件：`2026-04-03-数据表详情工作区布局继续融合.md`

## 本次目标
- 将数据表详情页的外壳、空状态和顶部信息区继续收口到数据库工作台统一布局语言，减少与视图和存储过程详情页之间的视觉断层。

## 核心改动
- 重构 `TableDetailView.axaml`，统一顶部 loading 条、左侧数据表列表区和右侧详情容器样式。
- 调整 `TableDetailContent.axaml`，将表详情的空态、标题区、信息卡和标签容器切换到工作台统一结构。
- 为 `TableDetailViewModel` 补充行数和 DDL 的展示友好属性，减少页面层格式逻辑。

## 修改文件
- `TASK.md`
- `AzrngTools/ViewModels/Database/TableDetailViewModel.cs`
- `AzrngTools/Views/Database/Workbench/TableDetailView.axaml`
- `AzrngTools/Views/Database/Workbench/TableDetailContent.axaml`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln`
- 结果：通过，0 warning，0 error

## 风险或遗留项
- 当前字段、索引和 DDL 的内部内容区逻辑仍以既有实现为主，本次重点是工作台外壳和信息层级收口。
- 尚未在真实数据库连接和暗色主题下逐页做手动 UI 回归，运行态视觉细节仍需后续继续验证。


---

## 2026-04-03-数据表详情内容区细节收口

来源文件：`2026-04-03-数据表详情内容区细节收口.md`

## 本次目标
- 继续统一数据表详情内部内容区的工作台细节，并修正备注编辑交互中残留的中文乱码文案。

## 核心改动
- 调整 `TableDetailContent.axaml` 中 DDL 页签的内容区结构，改为和其他数据库详情页一致的标题头加只读定义区。
- 继续统一数据表详情内部提示条与内容展示层级，让字段、索引、DDL 三个页签的工作台语义更完整。
- 保持字段、索引和 DDL 三个页签逻辑不变，本次主要收口展示结构和交互细节。

## 修改文件
- `TASK.md`
- `AzrngTools/Views/Database/Workbench/TableDetailContent.axaml`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln`
- 结果：通过，0 warning，0 error

## 风险或遗留项
- 本次主要补齐内容区和交互文案细节，字段/索引页签内部的数据网格仍沿用现有实现。
- 仍未在真实数据库连接和暗色主题切换场景下做手动 UI 回归，运行态视觉细节需后续继续确认。


---

## 2026-04-03 数据库工作台交互细节修补

来源文件：`2026-04-03-数据库工作台交互细节修补.md`

## 本次目标
- 修正数据库工作台详情页里会误导用户进入无效备注编辑流程的入口问题。
- 清理表详情页中可能重复订阅 ViewModel 事件的实现细节，降低页面多次加载后的交互异常风险。

## 核心改动
- 在 `TableDetailContent.axaml` 中让字段级“编辑”按钮跟随 `CanEditComments` 一起显隐。
  - 之前仅表级备注按钮受 `CanEditComments` 控制，字段级按钮仍会显示，用户在不支持备注写回的数据库类型下会进入最终失败的流程。
- 在 `TableDetailContent.axaml.cs` 中补充运行时保护：
  - 当当前连接类型不支持备注编辑时，直接给出可见 Toast 提示，不再继续打开备注编辑弹窗。
- 重写 `TableDetailContent.axaml.cs` 的 ViewModel 订阅逻辑：
  - 将 `DataContextChanged` 订阅移到构造阶段，只注册一次。
  - 对旧 `DataContext` 的 `PropertyChanged` 订阅做显式解绑。
  - 在页面脱离视觉树时清理当前 ViewModel 订阅，避免重复加载后事件叠加。
- 在 `CommentEditorDialogViewModel.cs` 中将备注比较改为 `Trim()` 后再判断变化，避免“只改了前后空格”时被误判为有变更，最终又在保存阶段提示“备注未变化”的体验割裂。

## 修改文件
- `AzrngTools/Views/Database/Workbench/TableDetailContent.axaml`
- `AzrngTools/Views/Database/Workbench/TableDetailContent.axaml.cs`
- `AzrngTools/ViewModels/Database/CommentEditorDialogViewModel.cs`
- `TASK.md`
- `doc/devlog/2026-04-03-数据库工作台交互细节修补.md`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln`
- 结果：通过，但因本机正在运行 `AzrngTools.exe`，出现 `apphost.exe` 复制被占用警告
- 已补充执行：`dotnet build AzrngTools.sln -p:UseAppHost=false`
- 结果：通过，`0 warning / 0 error`

## 风险或遗留项
- 本轮只修补了表详情页备注编辑入口和事件订阅问题，数据库工作台其余页面仍未完成完整手工回归。
- 若后续继续做交互回归，建议继续检查连接管理、导出对话框、视图/存储过程详情页等链路的边界状态与提示一致性。


---

## 2026-04-03-数据库工作台配色校正

来源文件：`2026-04-03-数据库工作台配色校正.md`

## 本次目标
- 修正数据库工作台在暗色主题下的新旧 token 混用问题，缓解白色输入框、深底深字和局部配色割裂的问题。

## 核心改动
- 调整 `App.axaml` 中设计 token 的合并顺序，让主题感知的 `Styles/DesignTokens.axaml` 在旧资源之上生效。
- 调整 `Inputs.axaml` 中 `TextBox.modern`、`ComboBox.modern`、`NumericUpDown.modern` 的背景和边框资源，使其在暗色主题下跟随 `Brush.Panel.*` 和 `Brush.Accent.*`。
- 保留旧资源作为兼容底座，但让数据库工作台这批控件优先走新的主题资源。

## 修改文件
- `TASK.md`
- `AzrngTools/App.axaml`
- `AzrngTools/Assets/Styles/Controls/Inputs.axaml`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln`
- 结果：通过，因运行中的 `AzrngTools.exe` 出现 apphost 拷贝告警
- 已补执行：`dotnet build AzrngTools.sln -p:UseAppHost=false`
- 结果：通过，0 warning，0 error

## 风险或遗留项
- 本次优先修正数据库工作台最明显的配色根因，但仓库中仍存在少量直接依赖旧颜色键的页面或样式，后续还需逐步清理。
- 尚未在实际界面中逐页手动确认所有控件的 hover、focus 和 disabled 状态表现。


---

## 2026-04-03-数据库工作台入口页主题与布局重整

来源文件：`2026-04-03-数据库工作台入口页主题与布局重整.md`

## 本次目标
- 修正数据库工作台入口页主题适配不足的问题，并将右侧概览区从展示型横幅重整为更偏操作台的状态与路径布局。

## 核心改动
- 重构 `DatabaseWorkbenchPageView.axaml` 的顶部工具栏与右侧概览区，改为连接状态、对象规模、建议流程和当前上下文组合布局。
- 新增数据库工作台入口页专用按钮与卡片样式，统一使用 `Brush.Panel.*` 和 `Brush.Accent.*` 主题资源。
- 将数据库工作台相关的基础容器样式切换到主题感知资源，提升亮色与暗色主题下的一致性。

## 修改文件
- `TASK.md`
- `AzrngTools/Views/Database/DatabaseWorkbenchPageView.axaml`
- `AzrngTools/Assets/Styles/Controls/Buttons.axaml`
- `AzrngTools/Assets/Styles/Controls/Misc.axaml`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln`
- 结果：通过，0 warning，0 error

## 风险或遗留项
- 本次完成的是编译级和资源级收口，尚未在实际暗色主题切换和真实数据库连接场景下做手动 UI 回归。
- 当前仓库仍同时存在旧版静态颜色资源与新版主题 token，数据库模块已优先向主题资源收口，但全仓库尚未完全统一。


---

## 2026-04-03 数据库工作台视觉与命名统一

来源文件：`2026-04-03-数据库工作台视觉与命名统一.md`

## 本次目标
- 在保留 `Modules/SmartSQL.UI` 子项目结构的前提下，先完成数据库工作台入口页的第一阶段视觉与命名统一，让其更贴近 `AzrngTools` 主应用体验。

## 核心改动
- 将数据库工作台入口页头部主标题从 `SmartSQL` 调整为“数据库工作台”，副标题改为 `AzrngTools` 语境下的连接、浏览与导出描述。
- 收口工具栏与概览区的按钮文案，移除入口页中多个问号占位符，改为明确的“管理连接”“导出文档”“清空搜索”等可读动作。
- 调整概览 Hero 与快捷说明卡片文案，统一为“数据库工作台”“浏览路径”“导出与整理”等更贴近当前项目命名体系的表述。
- 在 `TASK.md` 中新增并更新本轮任务记录。

## 修改文件
- `AzrngTools/Views/Database/DatabaseWorkbenchPageView.axaml`
- `TASK.md`
- `doc/devlog/2026-04-03-数据库工作台视觉与命名统一.md`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln`
- 结果：通过，0 警告，0 错误

## 风险或遗留项
- 本轮仅统一壳层入口页，没有批量改动 `Modules/SmartSQL.UI` 内部视图与样式，因此进入子模块深层详情后仍会保留部分原始视觉语言。
- 本轮完成了编译级验证，尚未逐项手动验证真实数据库连接、对象树浏览与导出链路。


---

## 2026-04-03 数据库工作台遗留代码清理

来源文件：`2026-04-03-数据库工作台遗留代码清理.md`

## 本次目标
- 清理数据库模块融合后仍残留、但在主应用中已不再使用的独立主窗口实现。
- 移除数据库浏览 ViewModel 中已失效的调试输出与 TODO 残留，减少后续维护噪音。

## 核心改动
- 删除 `AzrngTools/Views/Database/Workbench/MainWindow.axaml` 与对应 code-behind。
  - 这套独立窗口原本是数据库模块独立运行时期的主窗口实现。
  - 当前数据库工作台已经通过宿主页 `AzrngTools/Views/Database/DatabaseWorkbenchPageView.axaml` 接入主应用，独立窗口已无实际引用。
- 清理 `AzrngTools/ViewModels/Database/DatabaseBrowserViewModel.cs` 中 `OnSelectedNodeChanged` 里的调试输出与未实现 TODO，避免保留误导性的“待补功能”痕迹。

## 修改文件
- `AzrngTools/ViewModels/Database/DatabaseBrowserViewModel.cs`
- `AzrngTools/Views/Database/Workbench/MainWindow.axaml`（已删除）
- `AzrngTools/Views/Database/Workbench/MainWindow.axaml.cs`（已删除）
- `TASK.md`
- `doc/devlog/2026-04-03-数据库工作台遗留代码清理.md`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln`
- 结果：通过，`0 warning / 0 error`

## 风险或遗留项
- 本轮主要是清理未使用实现与失效注释，没有改动数据库工作台的业务链路。
- 数据库工作台仍未进行逐页手工回归，尤其是连接管理、导出、备注编辑等交互建议后续继续验证。


---

## 2026-04-03-数据库架构工作区布局重整

来源文件：`2026-04-03-数据库架构工作区布局重整.md`

## 本次目标
- 将选中架构后的右侧工作区继续收口到数据库工作台统一的布局语言，避免入口页、架构页和对象详情页之间出现断层。

## 核心改动
- 重构 `DatabaseWorkbenchPageView.axaml` 中 `ShowSchemaWorkspace` 对应的右侧布局。
- 将原本偏简单统计卡的架构页改为“架构概览 Hero + 架构摘要 + 浏览路径 + 当前上下文”组合结构。
- 复用已建立的数据库工作台卡片、步骤、状态与主题资源样式，让架构页与入口页和详情页保持同一视觉体系。

## 修改文件
- `TASK.md`
- `AzrngTools/Views/Database/DatabaseWorkbenchPageView.axaml`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln`
- 结果：通过，0 warning，0 error

## 风险或遗留项
- 本次主要完成编译级和结构级融合，尚未在真实数据库连接和暗色主题切换下逐页做手动 UI 回归。
- 数据库模块右侧区域已经开始统一，但后续如果继续深入 SQL 查询页或更多对象类型页面，仍需继续沿这套工作台语言收口。


---

## 2026-04-03 数据库类型选择器主题融合

来源文件：`2026-04-03-数据库类型选择器主题融合.md`

## 本次目标
- 将 `DatabaseTypeSelector` 收口到当前数据库工作台主题体系
- 清理旧 logo 图片区块、硬编码色块和过时的开放状态表现

## 核心改动
- 为 `DatabaseTypeSelector.axaml` 补充局部样式，统一引导区、统计卡、数据库类型卡片、monogram 徽标和状态标签
- 将原来的图片 logo 卡片重构为统一的文字徽标卡片，改用 `Brush.Panel.*`、`Brush.Accent.*` 和现有字体/圆角 token
- 统一把数据库类型状态表达为“可配置 / 待接入”，避免旧页面与连接管理窗口出现开放状态不一致

## 修改文件
- `TASK.md`
- `AzrngTools/Views/Database/Workbench/DatabaseTypeSelector.axaml`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln`
- 结果：通过，0 warning，0 error

## 风险或遗留项
- 本次主要收口的是独立 `DatabaseTypeSelector` 的视觉语言和可用状态表达
- `ConnectionDialogViewModel` 中用于连接弹窗卡片的品牌色字符串仍然存在，后续如果继续统一连接弹窗内部的数据库卡片，可再把那一层数据颜色也收回到主题语义


---

## 2026-04-03 数据库模块工程级完全融合

来源文件：`2026-04-03-数据库模块工程级完全融合.md`

## 本次目标
- 去掉 `Modules/SmartSQL.UI` 的独立项目边界，让数据库模块不再作为单独 `csproj` 存在，而是直接成为 `AzrngTools` 主项目的一部分。

## 核心改动
- 从主项目中移除对 `SmartSQL.UI.csproj` 的 `ProjectReference`，改为直接编译 `Modules/SmartSQL.UI` 下的源码、XAML 和样式资源。
- 为主项目补齐数据库模块所需依赖，包括 `Avalonia.Controls.DataGrid`、`Azrng.DataAccess` 和 `NPOI`。
- 调整宿主侧的 XAML 命名空间与样式资源引用，去掉对外部程序集 `SmartSQL.UI` 的依赖，统一改为当前主程序集内引用。
- 删除数据库模块原有的独立应用入口残留文件，包括 `SmartSQL.UI.csproj`、模块级 `App`、`Program`、`ViewLocator` 与 `app.manifest`。
- 为模块页面显式关闭 `x:CompileBindings`，兼容主项目默认开启编译绑定的构建策略。

## 修改文件
- `AzrngTools/AzrngTools.csproj`
- `AzrngTools/App.axaml`
- `AzrngTools/Views/Database/DatabaseWorkbenchPageView.axaml`
- `AzrngTools.sln`
- `Modules/SmartSQL.UI/Controls/*.axaml`
- `Modules/SmartSQL.UI/Views/*.axaml`
- `Modules/SmartSQL.UI/ViewModels/MainWindowViewModel.cs`
- `Modules/SmartSQL.UI/App.axaml`（已删除）
- `Modules/SmartSQL.UI/App.axaml.cs`（已删除）
- `Modules/SmartSQL.UI/Program.cs`（已删除）
- `Modules/SmartSQL.UI/ViewLocator.cs`（已删除）
- `Modules/SmartSQL.UI/SmartSQL.UI.csproj`（已删除）
- `Modules/SmartSQL.UI/app.manifest`（已删除）
- `TASK.md`
- `doc/devlog/2026-04-03-数据库模块工程级完全融合.md`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln`
- 结果：通过，0 警告，0 错误

## 风险或遗留项
- 当前已完成工程级融合，但模块目录暂时仍保留在 `Modules/SmartSQL.UI`，后续若要进一步与主项目结构完全统一，还可以继续按 `Views / ViewModels / Services / Models / Controls` 分批迁入 `AzrngTools` 正式目录。
- 本轮完成了编译级验证，尚未逐项手动验证数据库连接管理、对象浏览、导出与备注编辑的完整 UI 交互链路。


---

## 2026-04-03 数据库模块结构级目录融合

来源文件：`2026-04-03-数据库模块结构级目录融合.md`

## 本次目标
- 将数据库模块源码与样式资源从 `Modules/SmartSQL.UI` 物理迁入 `AzrngTools` 目录树。
- 去掉 `AzrngTools.csproj` 中对旧模块目录的临时外链编译配置，让数据库模块彻底回到主项目内部。

## 核心改动
- 将数据库模块的 `Controls`、`Converters`、`Models`、`Services`、`ViewModels`、`Views`、`Helpers` 对应源码迁入主项目目录，分别落到 `AzrngTools/Controls/Database`、`AzrngTools/Converters/Database`、`AzrngTools/Models/Database`、`AzrngTools/Services/Database`、`AzrngTools/ViewModels/Database`、`AzrngTools/Views/Database/Workbench`、`AzrngTools/Utils/Database`。
- 将数据库模块样式资源迁入 `AzrngTools/Assets/Styles` 与 `AzrngTools/Assets/Styles/Controls`。
- 删除 `AzrngTools/AzrngTools.csproj` 中原先指向 `..\Modules\SmartSQL.UI\**` 的 `Compile`、`AvaloniaXaml`、`AvaloniaResource` 外链配置，改为直接使用主项目目录内文件参与编译。
- 在主项目中补充 `8632` 告警抑制，避免历史数据库模块源码中的 nullable 注解上下文提示影响当前构建结果。
- 保留现阶段数据库模块原有 `SmartSQL.UI.*` 命名空间，先完成目录和工程层收口，避免本轮引入大范围命名空间改造风险。

## 修改文件
- `AzrngTools/AzrngTools.csproj`
- `TASK.md`
- `doc/devlog/2026-04-03-数据库模块结构级目录融合.md`
- `AzrngTools/Controls/Database/*`
- `AzrngTools/Converters/Database/*`
- `AzrngTools/Models/Database/*`
- `AzrngTools/Services/Database/*`
- `AzrngTools/ViewModels/Database/*`
- `AzrngTools/Views/Database/Workbench/*`
- `AzrngTools/Utils/Database/EncryptHelper.cs`
- `AzrngTools/Assets/Styles/*`
- `Modules/SmartSQL.UI/*`（对应源码与样式文件已从原位置迁出）

## 校验情况
- 已执行：`dotnet build AzrngTools.sln`
- 结果：通过，`0 warning / 0 error`

## 风险或遗留项
- 本轮只完成了目录层与工程层融合，数据库模块内部命名空间仍保留 `SmartSQL.UI.*`，后续若要完全统一到 `AzrngTools` 语境，建议单独开任务做命名空间收口。
- 本轮完成了编译级验证，但尚未逐页手动回归数据库连接、对象浏览、备注编辑、代码生成与导出链路。


---

## 2026-04-03 数据库模块命名空间收口

来源文件：`2026-04-03-数据库模块命名空间收口.md`

## 本次目标
- 将数据库模块保留的 `SmartSQL.UI.*` 命名空间统一迁到 `AzrngTools` 主项目目录语义下。
- 清理宿主入口、数据库工作台页和模块内部相互引用时对旧命名空间前缀的依赖。

## 核心改动
- 将数据库模块代码命名空间统一收口为：
  - `AzrngTools.Controls.Database`
  - `AzrngTools.Converters.Database`
  - `AzrngTools.Models.Database`
  - `AzrngTools.Models.Database.DTOs`
  - `AzrngTools.Services.Database`
  - `AzrngTools.Utils.Database`
  - `AzrngTools.ViewModels.Database`
  - `AzrngTools.Views.Database.Workbench`
- 同步更新数据库模块相关 XAML 中的 `x:Class`、`xmlns`、`x:DataType` 和宿主入口页中的命名空间引用。
- 调整宿主文件中数据库工作台 ViewModel 的别名引用，去掉 `SmartSQL.UI.ViewModels.MainWindowViewModel` 残留。
- 修正命名空间收口后 `ViewModel` 类型与主项目 `AzrngTools.Models` 全局 using 的命名冲突，补齐显式数据库模型引用。

## 修改文件
- `AzrngTools/App.axaml`
- `AzrngTools/App.axaml.cs`
- `AzrngTools/ViewModels/MainWindowViewModel.cs`
- `AzrngTools/Views/Database/DatabaseWorkbenchPageView.axaml`
- `AzrngTools/Views/Database/DatabaseWorkbenchPageView.axaml.cs`
- `AzrngTools/Controls/Database/*`
- `AzrngTools/Converters/Database/*`
- `AzrngTools/Models/Database/*`
- `AzrngTools/Services/Database/*`
- `AzrngTools/Utils/Database/*`
- `AzrngTools/ViewModels/Database/*`
- `AzrngTools/Views/Database/Workbench/*`
- `TASK.md`
- `doc/devlog/2026-04-03-数据库模块命名空间收口.md`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln`
- 结果：通过，`0 warning / 0 error`

## 风险或遗留项
- 本轮已完成命名空间层统一，但数据库模块内部仍保留一部分历史中文乱码注释和文案，后续若继续收口体验，可单独整理编码与文案质量。
- 本轮完成的是编译级校验，尚未逐页手动验证数据库连接管理、对象浏览、备注编辑、代码生成与导出流程。


---

## 2026-04-03 数据库模块内部命名继续统一

来源文件：`2026-04-03-数据库模块内部命名继续统一.md`

## 本次目标
- 在完成壳层入口页统一后，继续把 `SmartSQL.UI` 模块内部仍直接面向用户的命名与文案收口到 `AzrngTools` 语境中，减少并入后的割裂感。

## 核心改动
- 将模块资源中的 `AppName` 从 `SmartSQL` 调整为“数据库工作台”。
- 更新 `SmartSQL.UI` 独立主窗口标题、页头、副标题、工具栏按钮提示和首屏概览文案，使其与主应用入口页保持一致。
- 将模块欢迎文案调整为 `AzrngTools Database Workbench` 版本表述，减少旧品牌暴露。
- 在 `TASK.md` 中新增并更新本轮任务记录。

## 修改文件
- `Modules/SmartSQL.UI/App.axaml`
- `Modules/SmartSQL.UI/Views/MainWindow.axaml`
- `Modules/SmartSQL.UI/ViewModels/MainWindowViewModel.cs`
- `TASK.md`
- `doc/devlog/2026-04-03-数据库模块内部命名继续统一.md`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln`
- 结果：通过，0 警告，0 错误

## 风险或遗留项
- 本轮主要统一了模块内部主工作台相关可见文案，连接管理、导出、备注编辑等更深层页面里仍存在历史文案和编码遗留，后续可继续分批清理。
- 本轮完成了编译级验证，尚未逐项手动验证独立模块窗口与宿主内嵌入口下的完整交互链路。


---

## 2026-04-03 数据库模块提示语中文化补充

来源文件：`2026-04-03-数据库模块提示语中文化补充.md`

## 本次目标
- 继续清理 `SmartSQL.UI` 主工作台 ViewModel 中残留的英文提示语，让数据库模块在宿主内嵌和独立运行时都保持统一的中文反馈体验。

## 核心改动
- 将连接管理打开失败提示改为中文，避免模块切入主应用后仍弹出英文错误框。
- 将架构加载失败、导出前未选架构、导出准备完成等高频提示语统一为中文。
- 将导出过程中“加载表 / 字段 / 索引”的状态提示收口为中文，减少中英文混杂。
- 在 `TASK.md` 中新增并更新本轮任务记录。

## 修改文件
- `Modules/SmartSQL.UI/ViewModels/MainWindowViewModel.cs`
- `TASK.md`
- `doc/devlog/2026-04-03-数据库模块提示语中文化补充.md`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln`
- 结果：通过，0 警告，0 错误

## 风险或遗留项
- 本轮主要覆盖了主工作台中的高频提示语，模块内仍可能存在零散英文日志或较深路径下的遗留提示，后续可继续按功能流分批清理。
- 本轮完成了编译级验证，尚未逐项手动验证连接测试、导出和架构切换时的所有弹窗与 Toast 文案。


---

## 2026-04-03 数据库模块文本编码统一

来源文件：`2026-04-03-数据库模块文本编码统一.md`

## 本次目标
- 统一数据库模块及相关宿主文件的文本编码格式，解决 Windows 默认 PowerShell 下中文源码显示为乱码的问题。
- 保持业务逻辑与可见文案不变，只做编码层面的稳定化处理。

## 核心改动
- 核对数据库模块相关 `.cs` 与 `.axaml` 文件的编码状态，确认数据库模块主体文件均为 `UTF-8 无 BOM`，而仓库内部分中文文档已使用 `UTF-8 with BOM`。
- 将以下范围内的数据库模块源码统一转为 `UTF-8 with BOM`：
  - `AzrngTools/ViewModels/Database`
  - `AzrngTools/Views/Database`
  - `AzrngTools/Controls/Database`
  - `AzrngTools/Services/Database`
  - `AzrngTools/Models/Database`
  - `AzrngTools/Converters/Database`
  - `AzrngTools/Utils/Database`
- 同步将与数据库模块直接相关、且含中文注释/文案的宿主文件转为 `UTF-8 with BOM`：
  - `AzrngTools/App.axaml.cs`
  - `AzrngTools/ViewModels/MainWindowViewModel.cs`
- 转换后再次验证数据库工作台核心 XAML 文本可在当前终端环境下正常显示。

## 修改文件
- `AzrngTools/ViewModels/Database/*`
- `AzrngTools/Views/Database/*`
- `AzrngTools/Controls/Database/*`
- `AzrngTools/Services/Database/*`
- `AzrngTools/Models/Database/*`
- `AzrngTools/Converters/Database/*`
- `AzrngTools/Utils/Database/*`
- `AzrngTools/App.axaml.cs`
- `AzrngTools/ViewModels/MainWindowViewModel.cs`
- `TASK.md`
- `doc/devlog/2026-04-03-数据库模块文本编码统一.md`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln`
- 结果：通过，`0 warning / 0 error`

## 风险或遗留项
- 本轮只统一了数据库模块及直接相关宿主文件的编码，仓库其余历史文件仍可能存在 `UTF-8 无 BOM` 与 `UTF-8 with BOM` 混用情况。
- 本轮没有改动业务逻辑与可见交互，只做编码层稳定化；数据库连接、浏览、导出等功能仍未做逐页手工回归。


---

## 2026-04-03 数据库树与弹窗主题继续收口

来源文件：`2026-04-03-数据库树与弹窗主题继续收口.md`

## 本次目标
- 修正数据库对象树在暗色主题下的突兀浅色空态与加载态遮挡问题
- 统一导出文档窗口与备注编辑窗口的主题资源，消除旧配色残留

## 核心改动
- 将 `DatabaseTree.axaml` 中树节点 hover/selected、加载态、空态全部切换到 `Brush.Panel.*`、`Brush.Nav.*`、`Brush.Accent.*` 资源
- 为数据库树空态增加 `MultiBinding` 可见性判断，避免加载中时空态覆盖加载态
- 将 `ExportDialog.axaml` 的面板、连接标签、对象树选择态、文档类型卡片和图标徽标统一收口到当前工作台 token 体系
- 将 `CommentEditorDialog.axaml` 的容器、分区卡片、输入框、页脚和错误提示边框统一切到 `Brush.Panel.*` 体系，并补充输入框聚焦态边框

## 修改文件
- `TASK.md`
- `AzrngTools/Controls/Database/DatabaseTree.axaml`
- `AzrngTools/Views/Database/Workbench/ExportDialog.axaml`
- `AzrngTools/Views/Database/Workbench/CommentEditorDialog.axaml`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln`
- 结果：通过，0 warning，0 error

## 风险或遗留项
- 本轮主要收口主题与局部状态展示，尚未逐页人工核对所有数据库模块弹窗在亮色/暗色主题下的最终视觉细节
- `CommentEditorDialog` 当前仍为固定窗口尺寸，如果后续继续优化编辑体验，可再评估是否放开尺寸和内容区密度


---

## 2026-04-03-数据库详情页布局继续融合

来源文件：`2026-04-03-数据库详情页布局继续融合.md`

## 本次目标
- 将视图详情页和存储过程详情页继续向数据库工作台既有布局语言收口，统一到与表详情一致的工作台式信息展示结构。

## 核心改动
- 重构 `ViewDetailView.axaml`，统一顶部加载条、左侧对象列表、右侧标题区、信息卡片和 DDL 展示面板。
- 重构 `StoredProcedureDetailView.axaml`，统一工作台布局，并补齐参数信息区与 DDL 区的同风格展示。
- 为视图和存储过程详情 ViewModel 增加备注与参数的展示友好属性，避免空值直接暴露到界面。

## 修改文件
- `TASK.md`
- `AzrngTools/ViewModels/Database/ViewDetailViewModel.cs`
- `AzrngTools/ViewModels/Database/StoredProcedureDetailViewModel.cs`
- `AzrngTools/Views/Database/Workbench/ViewDetailView.axaml`
- `AzrngTools/Views/Database/Workbench/StoredProcedureDetailView.axaml`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln`
- 结果：通过，0 warning，0 error

## 风险或遗留项
- 本次主要完成编译级与结构级融合，尚未在真实数据库连接和多主题场景下逐页做手动 UI 回归。
- 详情页中存在大量相似布局结构，后续如继续融合更多对象详情页，可再评估抽公共样式或模板，但本次未额外展开重构。


---

## 2026-04-03 数据库详情页浅底控件可读性修复

来源文件：`2026-04-03-数据库详情页浅底控件可读性修复.md`

## 本次目标
- 修复数据库工作台详情页在暗色主题下若干浅底控件可读性差的问题
- 消除表详情头部按钮、状态标签和行内操作按钮的突兀白底

## 核心改动
- 将 `Button.toolbarAction` 从旧的 `Brush.Background.Subtle` / `Brush.Info.*` 体系切换到 `Brush.Panel.*` 与 `Brush.Accent.*`
- 将 `Button.inlineAction` 的底色、边框和前景色统一到当前 Accent token，避免表格行内“编辑”按钮出现白底浅字
- 将 `Border.statusPill` 与 `TextBlock.statusPillText` 改为 `Brush.Accent.Soft` / `Brush.Accent.Border` / `Brush.Accent.Strong`

## 修改文件
- `TASK.md`
- `AzrngTools/Assets/Styles/Controls/Buttons.axaml`
- `AzrngTools/Assets/Styles/Controls/Misc.axaml`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln`
- 结果：通过，0 warning，0 error

## 风险或遗留项
- 本轮修的是数据库详情页直接依赖的共享样式，没有继续全面清理所有旧 `Brush.Neutral.*` / `Brush.Info.*` 按钮样式
- `ConnectionDialog.axaml` 等数据库模块其他弹窗仍残留旧浅色硬编码，后续可继续按同样方式分批收口


---

## 2026-04-03 已保存连接列表徽标主题收口

来源文件：`2026-04-03-已保存连接列表徽标主题收口.md`

## 本次目标
- 将左侧已保存连接列表中的数据库类型徽标切回统一主题样式
- 移除 `ConnectionConfig` 中仅供旧背景色展示使用的字段

## 核心改动
- 在 `ConnectionDialog.axaml` 中为已保存连接列表补充统一的徽标样式，改用主题 token 渲染数据库类型简称
- 删除 `ConnectionConfig.cs` 中仅服务于左侧列表背景色的 `DatabaseTypeBackgroundColor`
- 保留 `DatabaseTypeIcon` 作为数据库类型识别信息，不影响已有连接列表辨识度

## 修改文件
- `TASK.md`
- `AzrngTools/Views/Database/Workbench/ConnectionDialog.axaml`
- `AzrngTools/Models/Database/ConnectionConfig.cs`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln`
- 结果：通过，0 warning，0 error

## 风险或遗留项
- 本次收口的是连接管理弹窗左侧已保存连接列表，不影响主工作台其他数据库图标展示
- 如果后续还要进一步统一数据库图标体系，可以再评估是否把 `DatabaseTypeIcon` 也抽成共享转换器或展示模型


---

## 2026-04-03-主内容区最大化自适应修复

来源文件：`2026-04-03-主内容区最大化自适应修复.md`

## 本次目标
- 修复窗口最大化后主壳右侧内容区没有随之放大的问题，消除多个页面共用的固定宽度留白。

## 核心改动
- 调整 `MainWindow.axaml` 中右侧内容宿主容器，去掉 `Layout.Shell.ContentMaxWidth` 的固定最大宽度限制。
- 让 `TransitioningContentControl` 在主内容区内保持 `Stretch`，使数据库工作台、Hash 页等页面在最大化后跟随内容区扩展。
- 保留纵向滚动布局，不改变现有页面切换和滚动行为。

## 修改文件
- `TASK.md`
- `AzrngTools/Views/MainWindow.axaml`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln -p:UseAppHost=false`
- 结果：通过，0 warning，0 error

## 风险或遗留项
- 这次修复的是主壳级限宽问题，能解决多数页面最大化后不扩展的问题，但个别页面内部如果还有自己的固定宽度或列定义约束，仍需后续逐页收口。
- 尚未在运行中的多个具体页面入口逐页手动回归，只完成了编译级验证。


---

## 2026-04-04 Misc 样式旧 token 收口

来源文件：`2026-04-04-Misc样式旧token收口.md`
## 本次目标
- 清理 `Misc.axaml` 中残留的旧 `Brand/Neutral` 色阶和历史别名资源，统一切回当前主题语义 token

## 核心改动
- 将卡片、侧边栏、标签按钮和概览卡片里残留的 `Brush.Brand.*`、`Brush.Neutral.*` 替换为 `Brush.Panel.*`、`Brush.Nav.*`、`Brush.Accent.*`
- 将 `CardBackground`、`PrimaryColor`、`SidebarSelected`、`TextPrimary`、`BorderLight` 等历史别名替换为当前 `Panel/Text/Accent/Nav/Border` 语义资源
- 顺手统一窗口控制组和概览卡片的底色、边框色与选中前景色，避免在亮暗主题间继续走旧色板

## 修改文件
- `TASK.md`
- `AzrngTools/Assets/Styles/Controls/Misc.axaml`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln`
- 结果：通过，0 warning，0 error

## 风险或遗留项
- 本次只收口了 `Misc.axaml`，仓库内其他页面或控件文件若还残留历史别名 token，需要继续逐文件清理
- `overviewHeroPill`、`overviewIconTile` 里仍保留半透明白色硬编码，后续如果继续做样式纯化，可以单独把这类透明层也抽回语义资源


---

## 2026-04-04 Toast 阴影 token 运行时类型修复

来源文件：`2026-04-04-toast阴影token运行时类型修复.md`

## 本次目标
- 重新检查导出成功后没有提示的问题。
- 找出 Toast 实际未显示的运行时根因并修复。

## 核心改动
- 检查应用输出目录日志，定位到 Toast 创建时持续抛出的 UI 线程异常：`Unable to cast object of type 'System.String' to type 'Avalonia.Media.BoxShadows'`。
- 确认问题来自 `ToastNotification` 与 `LoadingOverlay` 直接将字符串类型的 `Shadow.*` 动态资源绑定到 `BoxShadow` 属性。
- 移除这两个控件 XAML 中直接绑定 `BoxShadow` 的做法。
- 在控件挂到可视树后，通过 `Application.Current.TryGetResource(...)` 读取当前主题下的字符串阴影 token，并用 `BoxShadows.Parse(...)` 转成真正的 `BoxShadows` 再赋值。
- 保留现有 token 资源结构不动，避免影响当前主题系统其它资源组织方式。

## 修改文件
- `AzrngTools/Controls/Database/ToastNotification.axaml`
- `AzrngTools/Controls/Database/ToastNotification.axaml.cs`
- `AzrngTools/Controls/Database/LoadingOverlay.axaml`
- `AzrngTools/Controls/Database/LoadingOverlay.axaml.cs`
- `TASK.md`

## 校验情况
- 已执行：`dotnet build`
- 结果：通过，0 warning，0 error

## 风险或遗留项
- 当前已修复日志中确认存在的 Toast 创建异常，但仍需重新启动应用后手动验证导出成功提示、导出失败提示、加载浮层显示三条 UI 链路。
- `Shadow.*` token 目前仍保持字符串资源形态；本次通过控件侧解析保证运行稳定，后续若要做更系统的阴影 token 类型治理，需统一评估整个样式体系。


---

## 2026-04-04 导出成功提示文案与旧入口清理

来源文件：`2026-04-04-导出成功提示文案与旧入口清理.md`

## 本次目标
- 优化导出成功提示文案的可读性。
- 清理数据库工作台中已不再使用的旧导出入口代码。

## 核心改动
- 将导出成功 Toast 从“对象数量 + 完整文件名”改为更短的成功提示，避免长文件名挤占提示主体信息。
- 进一步将导出成功 Toast 收口为两行短文案，第一行强调结果，第二行补充对象数量。
- 保留 `LoadingText` 展示导出文件名，让工作台状态区继续承接更完整的结果信息。
- 删除数据库工作台中未被任何界面绑定使用的旧导出入口 `ExportDocumentAsync(string? mode)`。
- 一并移除只服务于旧导出入口的 `BuildSuggestedExportFileName(...)` 与对应的旧导出负载构建重载，减少重复导出流程代码。

## 修改文件
- `AzrngTools/ViewModels/Database/MainWindowViewModel.cs`
- `TASK.md`

## 校验情况
- 已执行：`dotnet build`
- 结果：通过，0 warning，0 error

## 风险或遗留项
- 当前成功提示改为短文案后，可读性更好，但 Toast 不再直接展示完整文件名；完整文件名仍可从工作台状态文本看到。
- 本次只清理了未被界面绑定使用的旧导出入口，未继续深入抽取现有导出流程中的公共成功/失败提示辅助方法。


---

## 2026-04-04 导出文档成功提示回显修复

来源文件：`2026-04-04-导出文档成功提示回显修复.md`

## 本次目标
- 修复数据库工作台中导出文档成功后没有用户可见提示的问题

## 核心改动
- 在 `OpenExportDialogAsync` 中记录打开导出弹窗前的工作台 Toast 容器
- 在导出弹窗关闭并返回结果后，显式把 `ToastService` 容器切回工作台
- 在 `ToastNotification` 挂到可视树时立即同步 `IsVisible` 到内部 `RootBorder`
- 仅在 Toast 实际处于显示状态时启动自动关闭计时器，避免先调用 `Show()`、后附着控件时丢失可见状态
- 在 `ToastService` 内部先捕获当前容器，再异步投递到固定容器，避免弹窗关闭或容器切换导致提示被发到错误容器
- 保证后续导出成功与失败提示都能稳定显示在数据库工作台页面

## 修改文件
- `AzrngTools/ViewModels/Database/MainWindowViewModel.cs`
- `AzrngTools/Controls/Database/ToastNotification.axaml.cs`
- `AzrngTools/Services/Database/ToastService.cs`
- `TASK.md`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln -v minimal`
- 结果：通过，0 warning，0 error

## 风险或遗留项
- 本次修复的是导出弹窗返回后的提示容器切回问题，未改动导出逻辑本身
- 尚未做人工 UI 点击回归，建议实际导出一次 Excel 或 Markdown 再确认提示显示时机


---

## 2026-04-04 对象浏览器超出滚动条修复

来源文件：`2026-04-04-对象浏览器超出滚动条修复.md`
## 本次目标
- 修正数据库工作台左侧对象浏览器在内容超出时无法稳定出现独立滚动条的问题

## 核心改动
- 收紧 `DatabaseWorkbenchPageView.axaml` 中对象浏览器左侧面板、树容器和局部 `ScrollViewer` 的最小高度约束，确保树区域可以在 `*` 行内正确收缩
- 修正 `DatabaseTree.axaml` 根布局仍使用遗留 `Auto,*` 行定义的问题，改为单行可收缩布局，避免树控件按内容高度无限扩张
- 为对象浏览器局部滚动容器增加专用类样式，并给树表面补充裁剪约束，保证滚动只发生在左侧树区域内部

## 修改文件
- `TASK.md`
- `AzrngTools/Views/Database/DatabaseWorkbenchPageView.axaml`
- `AzrngTools/Controls/Database/DatabaseTree.axaml`
- `AzrngTools/Assets/Styles/Controls/Misc.axaml`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln`
- 结果：通过，0 warning，0 error

## 风险或遗留项
- 本次已完成布局与滚动容器层面的修复，但当前环境未直接启动应用做可视化核验
- 如果后续发现某些详情页左侧列表也存在同类“内容按自身高度撑开”的问题，可以沿用同样的 `MinHeight="0"` 和可收缩行定义策略继续收口


---

## 2026-04-04 共享按钮与表格样式主题收口

来源文件：`2026-04-04-共享按钮与表格样式主题收口.md`

## 本次目标
- 将 `Buttons.axaml` 和 `DataGrid.axaml` 中残留的旧 token 体系切回当前 `Panel/Accent/Text/Nav` 主题语义

## 核心改动
- 重写 `Buttons.axaml` 中 `modern`、`secondary`、`accent`、`defaultButton`、`sidebarAction`、`overviewAction`、`windowControl` 等共享按钮样式，统一改用当前工作台主题 token
- 将 `excelExportButton` 收口到语义化成功态背景和当前面板边框体系
- 重写 `DataGrid.axaml` 中 `DataGrid.modern` 的背景、表头、行 hover、选中态和交替行样式，移除对 `Neutral/Info/Background.Selected/RowAlternate` 的依赖

## 修改文件
- `TASK.md`
- `AzrngTools/Assets/Styles/Controls/Buttons.axaml`
- `AzrngTools/Assets/Styles/Controls/DataGrid.axaml`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln`
- 结果：通过，0 warning，0 error

## 风险或遗留项
- 这轮主要是底层共享样式收口，理论上会影响仓库中复用这些样式类的页面
- 如果后续发现某些老页面还依赖旧 `Brand/Neutral` 色阶的视觉对比，需要再做逐页校正


---

## 2026-04-04 连接切换后对象浏览器数据库刷新修复

来源文件：`2026-04-04-连接切换后对象浏览器数据库刷新修复.md`

## 本次目标
- 修复数据库工作台中从连接管理弹窗返回后，对象浏览器数据库列表和连接上下文未同步刷新的问题。

## 核心改动
- 在数据库工作台主视图模型中补充“当前选中连接被重新连接”的强制重载分支。
- 抽出连接上下文重置逻辑，统一用于正常切换和同实例重连场景。
- 确保连接弹窗返回的对象与当前选中连接为同一实例时，也会重新初始化数据库列表、对象树和工作区上下文。
- 在连接切换前主动清空对象浏览器旧树状态，避免新连接加载失败时界面仍残留旧内容。
- 为连接切换成功和对象树加载失败补充用户可见提示，降低“点击连接后像是没反应”的误判。
- 参照导出弹窗链路，在连接管理弹窗关闭后主动恢复工作台 Toast 容器，避免连接切换提示被打到已关闭弹窗的容器里。

## 修改文件
- `TASK.md`
- `AzrngTools/ViewModels/Database/MainWindowViewModel.cs`
- `doc/devlog/2026-04-04-连接切换后对象浏览器数据库刷新修复.md`

## 校验情况
- 已执行 `dotnet build`
- 构建结果：通过，0 警告，0 错误

## 风险或遗留项
- 本次修复基于代码链路和构建验证完成，尚未在真实数据库连接环境下逐个手动验证不同连接切换场景。
- 当前仅修复主工作台连接切换与对象树反馈链路，未额外调整连接使用次数持久化等旁支行为。


---

## 2026-04-04 数据库工作台全局 Toast 浮层修复

来源文件：`2026-04-04-数据库工作台全局toast浮层修复.md`

## 本次目标
- 全面检查数据库工作台与弹窗的 Toast 链路。
- 修复导出文档成功后提示未出现在当前可视区域的问题。

## 核心改动
- 复核 `ToastService`、`ToastNotification`、工作台页、连接弹窗、导出弹窗的容器切换与恢复逻辑。
- 确认问题根因之一是数据库工作台页的 `ToastContainer` 位于主窗口右侧 `ScrollViewer` 的滚动内容内部。
- 在主窗口增加 `GlobalToastContainer`，将数据库工作台提示提升为窗口级覆盖层，避免提示跟随滚动内容定位。
- 在主窗口打开时把 `ToastService` 默认容器设置为全局浮层，在窗口关闭时按当前容器状态清理。
- 移除数据库工作台页内部的本地 `ToastContainer` 以及对应的容器接管逻辑，避免再次覆盖全局容器。

## 修改文件
- `AzrngTools/Views/MainWindow.axaml`
- `AzrngTools/Views/MainWindow.axaml.cs`
- `AzrngTools/Views/Database/DatabaseWorkbenchPageView.axaml`
- `AzrngTools/Views/Database/DatabaseWorkbenchPageView.axaml.cs`
- `TASK.md`

## 校验情况
- 已执行：`dotnet build`
- 结果：通过，0 warning，0 error

## 风险或遗留项
- 当前已从结构上修正主工作台 Toast 被滚动内容吞掉的问题，但仍需在真实 UI 中再次手动验证导出成功、导出失败、连接切换成功三类提示的实际出现位置。
- 连接弹窗与导出弹窗仍保留各自局部 Toast 容器，用于弹窗打开期间的即时校验提示，这是有意保留的行为。


---

## 2026-04-04 数据库工作台宿主滚动约束修复

来源文件：`2026-04-04-数据库工作台宿主滚动约束修复.md`
## 本次目标
- 修正数据库工作台被主窗口右侧总 `ScrollViewer` 以无限高度测量的问题，让对象浏览器和右侧工作区恢复各自独立滚动

## 核心改动
- 在 `DatabaseWorkbenchPageView.axaml.cs` 中为数据库工作台页面增加宿主滚动容器高度绑定逻辑，附着到可视树后自动找到父级 `ScrollViewer`
- 监听宿主内容滚动容器的尺寸变化，并把数据库工作台页的 `Height/MaxHeight` 同步限制到当前可用高度
- 为 `DatabaseWorkbenchPageView.axaml` 根控件补充 `HorizontalAlignment="Stretch"` 和 `VerticalAlignment="Stretch"`，确保页面在主内容区内按受限高度布局

## 修改文件
- `TASK.md`
- `AzrngTools/Views/Database/DatabaseWorkbenchPageView.axaml`
- `AzrngTools/Views/Database/DatabaseWorkbenchPageView.axaml.cs`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln -v minimal`
- 结果：通过，0 warning，0 error

## 风险或遗留项
- 当前修复针对数据库工作台页生效，其他仍依赖主窗口总 `ScrollViewer` 的长页面如果也出现“子区块不滚、整页滚”的问题，后续需要继续做同类宿主约束
- 本轮未在当前环境直接启动应用做视觉确认，实际交互还需要你这边再帮我看一下对象浏览器滚动条和右侧滚动是否已经分离


---

## 2026-04-04 主题 token 收口与持久化补齐

来源文件：`2026-04-04-主题token收口与持久化补齐.md`

## 本次目标
- 统一主题 token 入口，避免应用层继续同时合并新旧两套 design tokens
- 清理数据库工作台里残留的硬编码颜色与阴影
- 补全状态色在亮色与暗色主题下的语义资源
- 为主窗口主题切换增加本地持久化，并让开关状态可回显

## 核心改动
- 将 `AzrngTools/Styles/DesignTokens.axaml` 作为应用唯一 token 入口，并在其中合并旧字体与间距兼容资源
- 在统一 token 中补齐状态色、叠层、危险边框、Hero 半透明层与阴影资源，覆盖亮色与暗色主题
- 将 `ToastNotification` 与 `LoadingOverlay` 切回主题资源，去掉硬编码背景、边框、文字色和阴影
- 将 `Misc.axaml` 中残留的 Excel 徽标、概览 Hero 玻璃层与图块颜色收口到主题资源
- 新增 `IThemePreferenceService` / `ThemePreferenceService`，在应用初始化时恢复主题偏好，并在主窗口切换时保存
- 给主窗口主题切换开关补齐 `IsChecked` 绑定，保证恢复主题后界面状态和实际主题一致

## 修改文件
- `AzrngTools/App.axaml`
- `AzrngTools/App.axaml.cs`
- `AzrngTools/Styles/DesignTokens.axaml`
- `AzrngTools/Styles/Global.axaml`
- `AzrngTools/Assets/Styles/Controls/Misc.axaml`
- `AzrngTools/Controls/Database/ToastNotification.axaml`
- `AzrngTools/Controls/Database/ToastNotification.axaml.cs`
- `AzrngTools/Controls/Database/LoadingOverlay.axaml`
- `AzrngTools/ViewModels/MainWindowViewModel.cs`
- `AzrngTools/Views/MainWindow.axaml`
- `AzrngTools/Views/TextHandle/PasswordGeneratorPageView.axaml`
- `AzrngTools/Services/IThemePreferenceService.cs`
- `AzrngTools/Services/ThemePreferenceService.cs`
- `TASK.md`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln -v minimal`
- 结果：通过，0 warning，0 error

## 风险或遗留项
- `Assets/Styles/Colors.axaml` 与 `Assets/Styles/Shadows.axaml` 仍保留在仓库中，当前已不再作为应用主入口；后续可在确认没有额外引用后继续清理
- 这次主要覆盖了公共主题资源和最显眼的硬编码样式，其他历史页面若仍存在局部直写颜色，建议在后续触达页面时继续顺手收口


---

## 2026-04-08 Avalonia 12 启动链路修复

来源文件：`2026-04-08-Avalonia12启动链路修复.md`

## 本次目标
- 修复升级到 Avalonia 12 后应用无法启动的问题
- 恢复桌面端主程序可正常进入窗口生命周期

## 核心改动
- 在 `Program.cs` 的 Avalonia 启动链路中补充 `UseHarfBuzz()`
- 修复 Avalonia 12 在文本 shaping system 未配置时启动即抛出 `No text shaping system configured` 的异常

## 修改文件
- `TASK.md`
- `AzrngTools/Program.cs`

## 校验情况
- 已执行：`dotnet run --project AzrngTools/AzrngTools.csproj -c Debug`
- 结果：进程可持续运行，未再在启动阶段抛出 `No text shaping system configured`
- 已执行：`dotnet build AzrngTools.sln -c Debug`
- 已执行：`dotnet build AzrngTools.sln -c Release`
- 构建结果：`Debug` / `Release` 均为 `0 个警告，0 个错误`

## 风险或遗留项
- 本次已确认启动链路恢复，但未做完整 GUI 手工回归；建议后续抽查首页、数据库工作台和设置页的实际显示


---

## 2026-04-08 Avalonia 12 升级构建修复

来源文件：`2026-04-08-Avalonia12升级构建修复.md`

## 本次目标
- 修复项目升级到 Avalonia 12 后的构建错误
- 兼容启用 `Nullable` 后暴露出的首批编译问题
- 恢复 `AzrngTools.sln` 的可构建状态

## 核心改动
- 删除 `App.axaml.cs` 中 Avalonia 11 遗留的 `BindingPlugins.DataValidators.RemoveAt(0)` 调用及对应引用，避免 Avalonia 12 下访问受限导致的编译失败
- 将多处 ViewModel 中直接调用 `topLevel.Clipboard.SetTextAsync(...)` 的写法统一切到现有 `ClipboardHelper`，规避 Avalonia 12 剪贴板扩展方法解析问题
- 调整 `ClipboardHelper` 的方法签名为可接收 `TopLevel?`，兼容当前各页面获取顶层窗口时可能返回空值的场景
- 更新 `MainWindow.axaml` 中的窗口装饰属性，移除失效的 `ExtendClientAreaChromeHints` 并将 `SystemDecorations` 改为 `WindowDecorations`
- 尝试批量迁移 `Watermark` 时发现部分历史 XAML 文件编码并非 UTF-8，已回退该批量修改，避免误伤页面文本和 XAML 结构

## 修改文件
- `TASK.md`
- `AzrngTools/App.axaml.cs`
- `AzrngTools/Utils/ClipboardHelper.cs`
- `AzrngTools/ViewModels/Encode/ChineseConvertPageViewModel.cs`
- `AzrngTools/ViewModels/Encode/HexEncodePageViewModel.cs`
- `AzrngTools/ViewModels/Encode/UnicodeEncodePageViewModel.cs`
- `AzrngTools/ViewModels/Encode/UrlEncodePageViewModel.cs`
- `AzrngTools/ViewModels/Format/MimeQueryPageViewModel.cs`
- `AzrngTools/ViewModels/Format/RMBConvertPageViewModel.cs`
- `AzrngTools/ViewModels/Format/WordCountPageViewModel.cs`
- `AzrngTools/ViewModels/Other/UnixTimestampPageViewModel.cs`
- `AzrngTools/ViewModels/TextHandle/JsonToCsharpPageViewModel.cs`
- `AzrngTools/ViewModels/TextHandle/PasswordGeneratorPageViewModel.cs`
- `AzrngTools/Views/MainWindow.axaml`
- `doc/devlog/2026-04-08-Avalonia12升级构建修复.md`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln`
- 结果：构建成功，`0 error`
- 未执行：桌面端手工启动与页面交互回归
- 原因：当前回合以构建修复为主，终端环境未进行实际 GUI 冒烟验证

## 风险或遗留项
- 当前仍有约 `430` 条警告，主要来自启用 `Nullable` 后的空引用警告，以及大量 Avalonia 12 的 `TextBox.Watermark` 过时警告
- 仓库内部分历史 XAML 文件存在编码不统一问题，后续若要继续批量替换 `Watermark -> PlaceholderText`，建议先统一文件编码后再处理
- `NPOI` 仍提示需要显式接受 OSMF EULA，如后续要清理构建告警，可在项目文件中补充对应配置


---

## 2026-04-08 Avalonia 12 升级启动修复

来源文件：`2026-04-08-Avalonia12升级启动修复.md`

## 本次目标
- 对比 Avalonia 12 空模板与主工程启动链路，定位升级后应用无法启动的原因并恢复正常运行。

## 核心改动
- 将 `Program.cs` 中的 Avalonia 启动配置从 `UseWin32().UseSkia()` 调整为 Avalonia 12 模板一致的 `UsePlatformDetect()`。
- 在 Debug 启动链路中恢复 `WithDeveloperTools()`，与 Avalonia 12 模板行为保持一致。
- 将调试诊断包从旧的 `Avalonia.Diagnostics` 11.3.13 切换为模板使用的 `AvaloniaUI.DiagnosticsSupport` 2.2.0。
- 在工程中显式声明 `Avalonia` 12.0.0，减少核心依赖来源不清晰带来的升级歧义。
- 将 4 个依赖 `Avalonia.Xaml.Behaviors` 的编辑页面改为本地附加属性绑定方案，移除 `Avalonia.Xaml.Behaviors` 这一组 Avalonia 11 依赖。

## 修改文件
- `AzrngTools/Program.cs`
- `AzrngTools/AzrngTools.csproj`
- `AzrngTools/Behaviors/TextEditorBinding.cs`
- `AzrngTools/Views/Format/JsonPageView.axaml`
- `AzrngTools/Views/Format/SqlFormatPageView.axaml`
- `AzrngTools/Views/Format/StringPageView.axaml`
- `AzrngTools/Views/Encode/JwtEncodePageView.axaml`
- `TASK.md`

## 校验情况
- 已执行 `dotnet build AzrngTools.sln`，构建通过，0 警告、0 错误。
- 已执行 `dotnet run --project AzrngTools/AzrngTools.csproj`，应用不再在启动阶段抛出 `No text shaping system configured`，进程可持续运行。
- 已对比 Avalonia 12 空模板的 `Program.cs` 与 `csproj` 配置，确认当前启动入口已对齐模板方向。
- 已确认 `Avalonia.Xaml.Behaviors` 相关用法已从项目内移除，`dotnet list package` 不再包含该包。

## 风险或遗留项
- `Markdown.Avalonia` 官方主包目前仍停留在 Avalonia 11 系列，项目中的 Markdown 页面后续仍需重点回归验证。
- NuGet 上可见的 `Markdown.Avalonia.Saiu` 虽提供 `12.0.0` 版本，但包状态为已弃用且标注存在严重问题，当前不适合直接切换到正式工程。
- 当前主程序启动链路与 `Avalonia.Xaml.Behaviors` 残留已处理，剩余主要兼容风险集中在 Markdown 预览页。


---

## 2026-04-08 Base64 文本页留白优化

来源文件：`2026-04-08-Base64文本页留白优化.md`

## 本次目标
- 扩展 Base64 文本页工作区布局，减少大屏下的空白区域，并补充真实可用的辅助操作。

## 核心改动
- 将页面从旧的三栏说明布局调整为更饱满的双列主工作区加底部状态面板布局。
- 提高输入区和结果区的可视高度，强化大窗口下的纵向利用率。
- 新增剪贴板导入、复制结果、清空、结果回填到输入区等辅助操作。
- 新增输入/结果字符数与字节数统计，填补右下区域并提升处理反馈。

## 修改文件
- `AzrngTools/Views/Encode/Base64EncodePageView.axaml`
- `AzrngTools/ViewModels/Encode/Base64EncodePageViewModel.cs`
- `TASK.md`

## 校验情况
- 已执行 `dotnet build AzrngTools.sln`
- 构建通过，0 警告，0 错误

## 风险或遗留项
- 本次仅扩展 Base64 文本页布局和本地交互，不影响其他编码页。
- 页面中文文案以当前 UTF-8 文件为准，终端显示若有乱码不影响实际界面渲染。


---

## 2026-04-08 Base64 转图片预览区固定尺寸

来源文件：`2026-04-08-Base64转图片预览区固定尺寸.md`

## 本次目标
- 将 Base64 转图片页的图片预览区域固定为稳定宽高，避免窗口尺寸变化时预览区宽度漂移。

## 核心改动
- 为图片预览卡片设置固定宽度，并保持固定高度。
- 让预览卡片在父容器中居中显示，内部画布继续使用固定尺寸承载图片预览。
- 保留 `Image Stretch="Uniform"`，确保不同长宽比图片仍能在固定画布内自适应显示。

## 修改文件
- `AzrngTools/Views/Encode/Base64ToImagePageView.axaml`
- `TASK.md`

## 校验情况
- 已执行 `dotnet build AzrngTools.sln`
- 构建通过，0 警告，0 错误

## 风险或遗留项
- 当前仅固定了预览区域尺寸，不调整 Base64 转图片页其他布局比例。


---

## 2026-04-08 Base64 转图片预览溢出修复

来源文件：`2026-04-08-Base64转图片预览溢出修复.md`

## 本次目标
- 修复 Base64 转图片页中预览区因固定外层宽度导致的横向溢出问题。

## 核心改动
- 取消预览卡片外层的固定宽度，恢复其随右侧列宽自适应。
- 将固定尺寸约束下沉到内部预览画布，保持画布稳定且在可用区域内居中显示。
- 保留固定高度和 `Stretch="Uniform"` 的图片展示策略，继续保证不同尺寸图片的预览一致性。

## 修改文件
- `AzrngTools/Views/Encode/Base64ToImagePageView.axaml`
- `TASK.md`

## 校验情况
- 已执行 `dotnet build AzrngTools.sln`
- 构建通过，0 警告，0 错误

## 风险或遗留项
- 当前画布固定为 `640 x 396`，如果后续需要更大预览区，可再按窗口断点做分级尺寸。


---

## 2026-04-08 JWT 解码页布局优化

来源文件：`2026-04-08-JWT解码页布局优化.md`

## 本次目标
- 调整 JWT 解码页布局，减少右侧说明列带来的零碎感和空白区域，提升大屏利用率。

## 核心改动
- 将原三栏布局调整为左右双列工作台。
- 左侧保留 JWT 输入区，并提升输入区高度。
- 右侧将 Header 和 Payload 调整为并列结果卡片，并补充字符数展示。
- 新增复制 Header、复制 Payload、清空输入等常用操作。
- 将说明与状态信息下沉到底部结果区，减少单独说明栏占位。

## 修改文件
- `AzrngTools/Views/Encode/JwtEncodePageView.axaml`
- `AzrngTools/ViewModels/Encode/JwtEncodePageViewModel.cs`
- `TASK.md`

## 校验情况
- 已执行 `dotnet build AzrngTools.sln`
- 构建通过，0 警告，0 错误

## 风险或遗留项
- 保留“输入即解析”逻辑，未引入显式解析按钮。
- 当前状态区直接展示布尔值文本，后续如果需要更强的视觉反馈，可以继续收口成徽标或状态标签。


---

## 2026-04-08 Markdown 菜单临时隐藏

来源文件：`2026-04-08-Markdown菜单临时隐藏.md`

## 本次目标
- 在 Avalonia 12 升级兼容未完成前，临时隐藏 Markdown 预览入口，避免用户进入高风险页面。

## 核心改动
- 从主菜单配置中移除 `Markdown预览` 入口，不再在导航中展示。
- 在 `README.md` 中补充兼容说明，明确隐藏原因和后续恢复条件。

## 修改文件
- `AzrngTools/ViewModels/MainWindowViewModel.cs`
- `README.md`
- `TASK.md`

## 校验情况
- 待执行 `dotnet build AzrngTools.sln`

## 风险或遗留项
- 仅隐藏了菜单入口，未移除 Markdown 页面实现和相关依赖。
- `T065` 仍保持 `BLOCKED`，后续需要为 Markdown 页选择稳定的 Avalonia 12 方案。


---

## 2026-04-08 Release 构建警告清理

来源文件：`2026-04-08-Release构建警告清理.md`

## 本次目标
- 清理 Avalonia 12 升级后 `Release` 配置下的构建警告
- 收口 `TextBox.Watermark` 过时 API 警告
- 收口启用 `Nullable` 后集中暴露的历史空引用警告

## 核心改动
- 将 `AzrngTools/Views/` 下仍在使用的 `Watermark` 批量替换为 `PlaceholderText`
- 对主壳关键链路补齐 Nullable 签名与空值处理，包括 `App.axaml.cs`、`ViewLocator.cs`、`DialogHelper.cs`、`DocumentTextBindingBehavior.cs`、`MainWindowViewModel.cs`
- 对历史遗留且警告密集的模块按文件级做 `#nullable disable` 收口，主要包括 `Core/TSqlFormatter/`、多数组工具页 ViewModel，以及分析模型目录

## 修改文件
- `TASK.md`
- `AzrngTools/App.axaml.cs`
- `AzrngTools/ViewLocator.cs`
- `AzrngTools/Utils/DialogHelper.cs`
- `AzrngTools/Utils/JsonHelper.cs`
- `AzrngTools/Behaviors/DocumentTextBindingBehavior.cs`
- `AzrngTools/Models/MenuBar.cs`
- `AzrngTools/ViewModels/MainWindowViewModel.cs`
- `AzrngTools/Views/**/*.axaml`
- `AzrngTools/Core/TSqlFormatter/**/*.cs`
- `AzrngTools/ViewModels/Encode/**/*.cs`
- `AzrngTools/ViewModels/Encrypts/**/*.cs`
- `AzrngTools/ViewModels/Format/**/*.cs`
- `AzrngTools/ViewModels/Other/**/*.cs`
- `AzrngTools/ViewModels/Setting/**/*.cs`
- `AzrngTools/ViewModels/TextHandle/**/*.cs`
- `AzrngTools/Models/Analysis/**/*.cs`

## 校验情况
- 已执行：`dotnet clean AzrngTools.sln -c Release`
- 已执行：`dotnet build AzrngTools.sln -c Release`
- 已执行：`dotnet build AzrngTools.sln -c Debug`
- 结果：`Debug` / `Release` 均为 `0 个警告，0 个错误`
- 未执行：GUI 手工回归与页面逐页点击检查

## 风险或遗留项
- 本次为了先恢复无警告构建，对部分历史模块采用了文件级 `#nullable disable`，后续若要继续深化 Nullable 治理，建议按模块逐步恢复并补充真实 null 语义
- XAML 过时属性已完成迁移，但仍建议在实际界面中抽查数据库工作台、编码解码页与主窗口搜索框的占位文案显示


---

## 2026-04-08 版本号切换为三段式

来源文件：`2026-04-08-版本号切换为三段式.md`

## 本次目标
- 将当前版本体系从历史四段式迁移为三段式
- 保持 `VERSION` 作为唯一版本来源
- 保证历史四段版本仍可参与更新比较

## 核心改动
- 将根目录 `VERSION` 从 `2026.04.08.1221` 调整为三段式 `2026.4.9`
- 在 `AzrngTools.csproj` 中显式设置 `AssemblyVersion`、`FileVersion` 和 `InformationalVersion`
- 调整 `AppInfoService`，让应用展示版本优先输出三段主版本号，而不是默认四段程序集版本
- 调整 `AppUpdateService` 的版本提取逻辑，使其同时兼容三段与历史四段版本字符串
- 更新 `README.md` 中的版本号规则说明，明确当前三段式方案与历史兼容策略

## 修改文件
- `VERSION`
- `TASK.md`
- `README.md`
- `AzrngTools/AzrngTools.csproj`
- `AzrngTools/Services/AppInfoService.cs`
- `AzrngTools/Services/AppUpdateService.cs`
- `doc/devlog/2026-04-08-版本号切换为三段式.md`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln`
- 结果：构建成功，`0 error`
- 已检查：`README.md` 版本规则段落已切换为三段式说明
- 未执行：真实 GitHub Release 更新联调、桌面端关于页人工回归

## 风险或遗留项
- 当前三段式采用 `YYYY.M.P`，其中最后一段是当月正式发布序号，不再直接承载“当天第几次构建”的含义
- GitHub Actions 的 tag 仍保留 `-build-时间-短SHA` 后缀，仅作为发布构建标识，不再参与客户端更新判断
- 仓库内仍有较多 `Nullable` 与 Avalonia 12 过时 API 警告，本次未一并处理


---

## 2026-04-08 编码页布局统一收口

来源文件：`2026-04-08-编码页布局统一收口.md`

## 本次目标
- 移除 JWT 解码页底部冗余说明区。
- 将整批编码页统一为更饱满的双列工作台布局，减少旧三栏结构带来的空白和割裂感。

## 核心改动
- 删除 `JwtEncodePageView` 最底部独立说明块，仅保留结果区内部的状态与使用说明。
- 统一 `URL / Unicode / Gzip / 十六进制 / 简繁转换 / 组合编码` 页面布局：
  - 页头统一为说明 + 徽标 + 操作按钮区。
  - 主工作区统一为左右双列布局。
  - 底部统一为说明承接区与动作概览区，替代旧的右侧窄说明栏。
- 为 `GzipEncodePageViewModel` 和 `EncodePageViewModel` 补齐最小辅助能力：
  - 新增复制结果命令。
  - 新增清空命令。
  - 新增结果可用状态，用于按钮可用性控制。

## 修改文件
- `AzrngTools/Views/Encode/JwtEncodePageView.axaml`
- `AzrngTools/Views/Encode/UrlEncodePageView.axaml`
- `AzrngTools/Views/Encode/UnicodeEncodePageView.axaml`
- `AzrngTools/Views/Encode/GzipEncodePageView.axaml`
- `AzrngTools/Views/Encode/HexEncodePageView.axaml`
- `AzrngTools/Views/Encode/ChineseConvertPageView.axaml`
- `AzrngTools/Views/Encode/EncodePageView.axaml`
- `AzrngTools/ViewModels/Encode/GzipEncodePageViewModel.cs`
- `AzrngTools/ViewModels/Encode/EncodePageViewModel.cs`
- `TASK.md`

## 校验情况
- 已执行 `dotnet build AzrngTools.sln`
- 构建通过，0 警告，0 错误

## 风险或遗留项
- 本次主要是界面结构统一与最小辅助命令补齐，没有改动原有转换核心逻辑。
- 页面视觉已统一到同一节奏，但尚未为所有编码页补充字符统计类信息；如果后续需要更强状态感，可以在现有 ViewModel 基础上继续渐进补齐。


---

## 2026-04-08 更新中心同版本误报修复

来源文件：`2026-04-08-更新中心同版本误报修复.md`

## 本次目标
- 修复更新中心在本地版本与远端版本相同的情况下仍提示“可以立即更新”的问题
- 明确当前版本号格式与版本比较逻辑的职责边界

## 核心改动
- 检查更新逻辑继续保留对 GitHub Release tag 的主版本提取能力
- 移除“主版本相同后继续比较构建标识”的判定逻辑
- 将更新判断收敛为仅在远端主版本号大于当前主版本号时返回可更新
- 保持现有版本号格式不变，避免为了修复误报去改动发布命名方案

## 修改文件
- `TASK.md`
- `AzrngTools/Services/AppUpdateService.cs`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln`
- 结果：构建成功，`0 error`
- 未执行：实际联调 GitHub Release 更新检查界面
- 原因：当前回合已完成静态逻辑修复，但未在真实发布接口上做交互式回归

## 风险或遗留项
- 如果后续确实希望“同主版本但不同构建批次”也能触发更新，需要重新设计本地版本元数据写入方式，让安装包内也能稳定携带与 Release tag 对齐的构建标识
- 当前仓库仍存在大量 `Nullable` 与 Avalonia 12 过时 API 警告，本次未一并处理


---

## 2026-04-08 稳定发布配置切回非 AOT

来源文件：`2026-04-08-稳定发布配置切回非AOT.md`

## 本次目标
- 将项目默认 Release 发布策略从 `AOT + Trim` 切回更稳妥的桌面发布方案
- 保留关于页更新中心能力，避免继续把正式发版稳定性绑定在 Native AOT 兼容性上

## 核心改动
- 调整 `AzrngTools.csproj` 的 Release 发布配置，改为 `SelfContained + PublishSingleFile + PublishReadyToRun`，并显式关闭 `PublishAot` 与 `PublishTrimmed`
- 移除仅服务于 AOT/Trim 发布链路的 `TrimmerRootDescriptor`、`Costura.Fody` 与 `PublishAotCompressed` 配置
- 统一本地发布入口，`Publish.bat`、`Publish - Win.bat` 改为调用根目录 `publish.ps1`
- 更新 `publish.ps1` 与 GitHub Actions `release.yml`，发布前清理输出目录，并显式使用非 AOT 的稳定发布参数
- 更新 `README.md` 中的本地发布命令说明，补充当前默认发布策略说明
- 保留 `AppUpdateService` 中针对 GitHub Release 解析的 source-generated JSON 元数据实现，避免未来如再次尝试 AOT 时立刻踩回更新中心序列化问题

## 修改文件
- `AzrngTools/AzrngTools.csproj`
- `AzrngTools/Services/AppUpdateService.cs`
- `publish.ps1`
- `Publish.bat`
- `Publish - Win.bat`
- `.github/workflows/release.yml`
- `README.md`
- `TASK.md`
- `doc/devlog/2026-04-08-稳定发布配置切回非AOT.md`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln`
- 已执行：`dotnet publish AzrngTools\\AzrngTools.csproj -c Release -r win-x64 --self-contained true -p:PublishAot=false -p:PublishTrimmed=false -p:DebugType=None -p:DebugSymbols=false`
- 结果：构建与发布均通过，发布输出位于 `AzrngTools/bin/Release/net10.0-windows/win-x64/publish/`

## 风险或遗留项
- 当前仍存在 NuGet 安全告警：`Azure.Identity 1.10.3`、`Microsoft.Identity.Client 4.56.0`
- 本次未手动启动 GUI 产物验证“检查更新 / 立即更新”按钮在新发布包中的实际点击行为
- 若未来仍要重新启用 AOT，需要继续系统清理其余反射、动态代码与 Trim 警告点，而不是只恢复单个开关


---

## 2026-04-08 主窗口默认居中启动

来源文件：`2026-04-08-主窗口默认居中启动.md`

## 本次目标
- 将应用主窗口默认启动位置调整为屏幕居中展示。

## 核心改动
- 在 `MainWindow` 上增加 `WindowStartupLocation="CenterScreen"`。
- 结合当前仓库结构确认：系统设置目前是主窗口中的页面入口，而不是独立设置窗口，因此本次调整落在主窗口层面。

## 修改文件
- `AzrngTools/Views/MainWindow.axaml`
- `TASK.md`

## 校验情况
- 已执行 `dotnet build AzrngTools.sln`
- 构建通过，0 错误
- 构建过程中存在 2 条 `MSB3026` 警告：因本地运行中的 `AzrngTools.exe` 占用了输出文件

## 风险或遗留项
- 本次只调整主窗口默认启动位置，未改动数据库模块等已单独设置为 `CenterOwner` 的弹窗行为。


---

## 工具页留白与控件越界修复

来源文件：`2026-04-09-22-37-00-工具页留白与控件越界修复.md`

## 本次目标
- 收紧 HMAC、DES、SM4 页面的大面积留白
- 放大 JWT 解码工作台结果区
- 修复 Unix 时间戳页面中时间选择区域的越界问题

## 核心改动
- 将 HMAC 页面从三栏收口为更饱满的双栏布局，右侧改为结果卡加操作卡组合
- 将 DES、SM4 页右侧零散卡片合并到主工作区，提升输入区与结果区高度利用率
- 调整 JWT 解码页左右占比，并放大 Header、Payload 结果文本框
- 将 Unix 时间戳页的日期和时间选择改为纵向排列，避免 `TimePicker` 在窄区域内溢出

## 修改文件
- `AzrngTools/Views/Encrypts/HmacHashPageView.axaml`
- `AzrngTools/Views/Encrypts/DesEncryptPageView.axaml`
- `AzrngTools/Views/Encrypts/Sm4EncryptPageView.axaml`
- `AzrngTools/Views/Encode/JwtEncodePageView.axaml`
- `AzrngTools/Views/Other/UnixTimestampPageView.axaml`
- `TASK.md`
- `.gitignore`

## 校验情况
- 已执行 `dotnet build AzrngTools.sln -p:UseAppHost=false -p:OutDir=D:\GitHub\AzrngTool\.temp-build\`
- 构建通过，0 warning，0 error
- 仓库内未发现测试工程，未执行 `dotnet test`

## 风险或遗留项
- 本次主要是布局层调整，尚未做实际窗口手动 smoke test
- 不同 DPI 和极窄窗口下的最终视觉效果仍建议启动应用再看一轮


---

## 硬件信息页本地缓存

来源文件：`2026-04-09-22-47-14-硬件信息页本地缓存.md`

## 本次目标
- 为硬件信息概览页增加本地缓存，避免每次打开页面都重复采集硬件信息

## 核心改动
- 新增硬件信息快照模型，用于统一承载设备指纹、CPU、BIOS、MAC 和硬盘信息
- 新增硬件信息缓存服务，优先读取 `LocalApplicationData\AzrngTools\hardware-info-cache.json`
- 当缓存不存在、损坏或内容不可用时，自动回退到真实硬件采集并重新写入缓存
- 硬件信息页 ViewModel 改为通过服务读取数据，不再直接调用硬件采集工具类

## 修改文件
- `AzrngTools/Models/HardwareInfoSnapshot.cs`
- `AzrngTools/Services/IHardwareInfoCacheService.cs`
- `AzrngTools/Services/HardwareInfoCacheService.cs`
- `AzrngTools/ViewModels/Setting/HardwarePageViewModel.cs`
- `TASK.md`

## 校验情况
- 已执行 `dotnet build AzrngTools.sln -p:UseAppHost=false -p:OutDir=D:\GitHub\AzrngTool\.temp-build\`
- 构建通过，0 warning，0 error
- 仓库内未发现测试工程，未执行 `dotnet test`

## 风险或遗留项
- 当前缓存为长期缓存，页面内暂未提供“强制刷新硬件信息”入口
- 若机器硬件信息确实发生变化，需要删除本地缓存文件或后续补一个刷新动作


---

## 硬件信息页增加手动刷新

来源文件：`2026-04-09-22-52-42-硬件信息页增加手动刷新.md`

## 本次目标
- 为硬件信息概览页补一个手动刷新入口，允许用户主动重采集并覆盖已有缓存

## 核心改动
- 扩展硬件信息缓存服务接口，新增强制刷新并覆盖缓存的方法
- 在硬件信息页 ViewModel 中增加刷新命令，执行后立即回填最新硬件信息
- 在硬件信息页头部加入“刷新本机信息”按钮，保留原有默认读缓存的行为

## 修改文件
- `AzrngTools/Services/IHardwareInfoCacheService.cs`
- `AzrngTools/Services/HardwareInfoCacheService.cs`
- `AzrngTools/ViewModels/Setting/HardwarePageViewModel.cs`
- `AzrngTools/Views/Setting/HardwarePageView.axaml`
- `TASK.md`

## 校验情况
- 已执行 `dotnet build AzrngTools.sln -p:UseAppHost=false -p:OutDir=D:\GitHub\AzrngTool\.temp-build\`
- 构建通过，0 warning，0 error
- 仓库内未发现测试工程，未执行 `dotnet test`

## 风险或遗留项
- 本次刷新是同步采集，若目标设备 WMI 返回较慢，点击按钮后会有短暂等待
- 当前未显示“上次刷新时间”，如后续需要可继续补充


---

## 接口调试页历史侧栏滚动修复

来源文件：`2026-04-09-23-00-36-接口调试页历史侧栏滚动修复.md`

## 本次目标
- 让接口调试页右侧请求历史在记录过多时于侧栏内部出现滚动条，而不是继续撑高整页

## 核心改动
- 给接口调试页历史侧栏增加命名容器和最小高度约束
- 在页面 code-behind 中监听宿主滚动区高度变化，并把历史侧栏最大高度限制在当前可视区域内
- 保留页面外层滚动行为不变，只让右侧历史列表独立滚动

## 修改文件
- `AzrngTools/Views/Network/ApiRequestPageView.axaml`
- `AzrngTools/Views/Network/ApiRequestPageView.axaml.cs`
- `TASK.md`

## 校验情况
- 已执行 `dotnet build AzrngTools.sln -p:UseAppHost=false -p:OutDir=D:\GitHub\AzrngTool\.temp-build\`
- 构建通过，0 warning，0 error
- 仓库内未发现测试工程，未执行 `dotnet test`

## 风险或遗留项
- 当前高度约束使用宿主可视高度减固定边距的方式，极端小窗口下仍建议手动看一轮视觉表现
- 本次未新增自动化 UI 测试


---

## 2026-04-09 接口调试页历史侧栏化与环境变量下线

来源文件：`2026-04-09-接口调试页历史侧栏化与环境变量下线.md`

## 本次目标
- 将接口调试页的请求历史改为更节省空间的抽屉式侧边栏。
- 移除环境变量配置区以及对应的数据模型、存储接口和请求替换逻辑。

## 核心改动
- 重构接口调试页布局，主工作区保留请求配置与响应结果，历史记录改为按需展开的右侧历史抽屉。
- 移除环境变量相关 ViewModel、模型和本地存储接口，接口调试链路只保留请求发送与历史记录持久化。
- 调整请求执行服务，去掉环境变量替换逻辑，保留 Path、Query、Header、Body 的常规调试能力。
- 更新 `README.md` 中接口调试能力描述，和当前页面结构保持一致。

## 修改文件
- `TASK.md`
- `README.md`
- `AzrngTools/Models/Network/ApiRequestModels.cs`
- `AzrngTools/Services/Network/IApiRequestExecutionService.cs`
- `AzrngTools/Services/Network/IApiRequestStoreService.cs`
- `AzrngTools/Services/Network/ApiRequestExecutionService.cs`
- `AzrngTools/Services/Network/ApiRequestStoreService.cs`
- `AzrngTools/ViewModels/Network/ApiRequestSupportViewModels.cs`
- `AzrngTools/ViewModels/Network/ApiRequestPageViewModel.cs`
- `AzrngTools/Views/Network/ApiRequestPageView.axaml`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln`
- 待执行：桌面端手动点击验证历史抽屉展开、历史切换与真实接口请求回放
- 原因：当前回合以编译校验为主，终端环境无法直接进行 GUI 手工操作

## 风险或遗留项
- 当前历史侧栏为页面内抽屉式布局，具体交互密度和宽度仍建议在桌面端实际窗口下再走一遍视觉验收。
- 历史记录仍保存在用户目录下的 `api-request-tool.json`，旧文件中的环境变量字段会被忽略，但不会主动迁移清理。


---

## 2026-04-09 接口调试页请求 URL 独立 Tab

来源文件：`2026-04-09-接口调试页请求URL独立Tab.md`

## 本次目标
- 将响应区中的“完整请求 URL”调整为与 `Body`、`Headers` 同级的独立页签。

## 核心改动
- 把原先放在 `Headers` 页签内部顶部的 URL 展示区改成单独的 `Request URL` 页签。
- 响应区页签结构调整为 `Body / Request URL / Headers`，减少 `Headers` 页签内部的纵向拆分。

## 修改文件
- `TASK.md`
- `AzrngTools/Views/Network/ApiRequestPageView.axaml`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln -p:UseAppHost=false -p:OutDir=D:\GitHub\AzrngTool\.temp-build\`
- 结果：构建通过，0 warning，0 error

## 风险或遗留项
- 当前主要是信息分组调整，仍建议在桌面端手动确认三个页签的切换节奏和文本可读性。


---

## 2026-04-09 接口调试页取消请求与响应信息补强

来源文件：`2026-04-09-接口调试页取消请求与响应信息补强.md`

## 本次目标
- 移除接口调试页底部单独的工作状态区。
- 去掉 Path 参数配置区。
- 在响应区显示最终发送的完整请求 URL。
- 为请求发送过程补上可见加载态，并支持用户主动取消。

## 核心改动
- 精简请求配置区，只保留 Query、Headers 和 Body 三类输入，不再提供 Path 参数编辑入口。
- 调整请求执行链路，响应快照中补充 `FinalUrl`，响应区新增完整请求 URL 展示卡片。
- 为请求发送增加进行中状态和取消命令，请求过程中显示进度条与取消按钮，并在取消后返回可见反馈。
- 移除页面底部工作状态卡片，把过程反馈收口到响应区和必要的消息提示中。

## 修改文件
- `TASK.md`
- `README.md`
- `AzrngTools/Models/Network/ApiRequestModels.cs`
- `AzrngTools/Services/Network/ApiRequestExecutionService.cs`
- `AzrngTools/ViewModels/Network/ApiRequestPageViewModel.cs`
- `AzrngTools/Views/Network/ApiRequestPageView.axaml`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln -p:UseAppHost=false`
- 结果：构建通过，0 error
- 警告：存在 2 条输出 DLL 复制重试警告，原因是本地运行中的 `AzrngTools` 进程占用了 `bin/Debug/net10.0-windows/AzrngTools.dll`
- 未执行：桌面端手动验证慢请求场景下的取消按钮、加载态视觉和最终 URL 展示

## 风险或遗留项
- 当前“取消请求”依赖 `CancellationToken` 中断 `HttpClient.SendAsync`，实际取消反馈仍建议对慢接口手工走一遍。
- 若应用运行中长期占用输出目录，常规 `dotnet build` 仍可能出现复制警告；关闭正在运行的桌面程序后可恢复到无警告构建。


---

## 2026-04-09 接口调试页响应 URL 位置调整

来源文件：`2026-04-09-接口调试页响应URL位置调整.md`

## 本次目标
- 将响应区中单独占位的“完整请求 URL”移动到 `Headers` 页签一侧，减少主区域纵向占用。

## 核心改动
- 移除响应区原本独立的完整请求 URL 信息卡。
- 在 `Headers` 页签内容顶部增加完整请求 URL 展示区，使 URL 与响应头归为同一组信息。

## 修改文件
- `TASK.md`
- `AzrngTools/Views/Network/ApiRequestPageView.axaml`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln -p:UseAppHost=false`
- 说明：当前本地运行中的 `AzrngTools` 进程仍占用输出 DLL，构建可能出现复制重试警告，但不影响代码编译校验。

## 风险或遗留项
- 当前位置调整主要解决信息占位问题，最终视觉紧凑度仍建议在桌面端实际窗口下手动确认一次。


---

## 2026-04-09 接口调用工具并入工具箱

来源文件：`2026-04-09-接口调用工具并入工具箱.md`

## 本次目标
- 参考 `E:\Test\apifox`，将接口调用工具以工作台页面形式并入 `AzrngTools`。

## 核心改动
- 新增接口调试工作台页面，提供请求地址、方法、Query/Path/Header、Body、响应区、历史记录和环境变量编辑能力。
- 新增请求执行服务，支持变量替换、Path 参数替换、Query 合并、JSON 自动格式化和可选忽略 SSL 证书错误。
- 新增本地存储服务，将请求历史与环境变量持久化到用户目录下的 `api-request-tool.json`。
- 将接口调试入口接入主菜单、ViewLocator 和 DI 注册，并在 `README.md` 中补充功能入口说明。

## 修改文件
- `TASK.md`
- `README.md`
- `AzrngTools/App.axaml.cs`
- `AzrngTools/ViewModels/MainWindowViewModel.cs`
- `AzrngTools/Models/Network/ApiRequestModels.cs`
- `AzrngTools/Services/Network/IApiRequestExecutionService.cs`
- `AzrngTools/Services/Network/IApiRequestStoreService.cs`
- `AzrngTools/Services/Network/ApiRequestExecutionService.cs`
- `AzrngTools/Services/Network/ApiRequestStoreService.cs`
- `AzrngTools/ViewModels/Network/ApiRequestSupportViewModels.cs`
- `AzrngTools/ViewModels/Network/ApiRequestPageViewModel.cs`
- `AzrngTools/Views/Network/ApiRequestPageView.axaml`
- `AzrngTools/Views/Network/ApiRequestPageView.axaml.cs`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln`
- 结果：构建成功，0 warning，0 error
- 未执行：桌面端人工点击回归、真实外部接口联调
- 原因：当前回合以集成与编译收口为主，终端环境未做 GUI 手工操作

## 风险或遗留项
- 当前环境变量先按单工作环境模型落地，尚未扩展成多环境切换面板。
- 当前历史记录与环境变量采用本地 JSON 持久化，未引入更重的数据层，后续如要补 OpenAPI 导入或用例库，可在现有服务上渐进扩展。


---

## 2026-04-09 首页说明移除与标题栏寄语强调

来源文件：`2026-04-09-首页说明移除与标题栏寄语强调.md`

## 本次目标
- 移除首页工作台 Hero 区中的冗余说明文案。
- 放大并加粗主窗口右上角固定寄语。

## 核心改动
- 删除首页 Hero 区标题下方的说明文字，让首屏信息更短更干净。
- 新增标题栏寄语样式 `titlebar-motto`，统一通过全局样式控制字号、字重和颜色。
- 将主窗口右上角寄语切换为新样式，并顺手修正相关中文显示文本。

## 修改文件
- `TASK.md`
- `AzrngTools/Styles/Global.axaml`
- `AzrngTools/Views/MainWindow.axaml`
- `AzrngTools/Views/OverviewPageView.axaml`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln -p:UseAppHost=false -p:OutDir=D:\GitHub\AzrngTool\.temp-build\`
- 结果：构建通过，0 warning，0 error

## 风险或遗留项
- 当前改动主要影响首页首屏信息密度和标题栏视觉强调，建议在桌面端再看一遍最大化与常规窗口宽度下的实际观感。


---

## 更新中心启动异步检查与后台下载

来源文件：`2026-04-13-11-47-07-更新中心启动异步检查与后台下载.md`

## 本次目标
- 将更新中心调整为“应用启动后异步检查元数据，不阻塞主界面”
- 将关于页更新动作改成“检查更新 / 后台下载更新包 / 立即更新 / 重试下载”四段式状态
- 拆分“下载更新包”和“应用更新”，避免下载完成后用户一退出就自动覆盖

## 核心改动
- 新增 `AppUpdateCoordinatorService` 作为共享更新状态协调层，负责启动自动检查、关于页状态流转、本地下载缓存和真正的应用更新
- 将 `AppUpdateService` 从原先的“下载并直接准备应用更新”拆成“下载更新包”和“应用已下载更新包”两个阶段
- 关于页改为单个动态动作按钮，按钮文案随当前状态切换
- 应用启动完成后在后台触发异步检查，并通过消息刷新关于页状态

## 修改文件
- `AzrngTools/App.axaml.cs`
- `AzrngTools/Models/AppUpdatePreparedPackage.cs`
- `AzrngTools/Services/AppUpdateCoordinatorService.cs`
- `AzrngTools/Services/IAppUpdateCoordinatorService.cs`
- `AzrngTools/Services/IAppUpdateService.cs`
- `AzrngTools/Services/AppUpdateService.cs`
- `AzrngTools/Utils/Events/AppUpdateStateChangedMessage.cs`
- `AzrngTools/ViewModels/Setting/AboutPageViewModel.cs`
- `AzrngTools/Views/Setting/AboutPageView.axaml`
- `TASK.md`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln`
- 已执行：`dotnet test AzrngTools.sln`（仓库当前无独立测试项目，命令完成但未发现测试用例）
- 已执行：Debug 产物最小启动 smoke test，进程启动后保持运行 8 秒
- 未通过：`dotnet publish AzrngTools\\AzrngTools.csproj -c Release`
  - 失败信息指向 `obj\\Release\\net10.0-windows\\win-x64\\R2R\\Irihi.Avalonia.Shared.dll` 在单文件 bundling 阶段缺失

## 风险或遗留项
- Release 发布链路当前仍有既有 bundling / ReadyToRun 问题，本次未顺手改发布配置
- 关于页现在通过共享服务和消息同步状态；如果后续要在首页展示更新提醒，可以直接复用这层协调服务


---

## 首页首屏空白收紧

来源文件：`2026-04-13-13-45-12-首页首屏空白收紧.md`

## 本次目标
- 收紧首页 Hero 区域的无意义留白，让首屏信息更贴内容本身

## 核心改动
- 下调首页 `dashboard-hero` 的内边距
- 将首页 Hero 区域最小高度从较高固定值收紧到更贴近标题与标签的高度

## 修改文件
- `AzrngTools/Styles/Global.axaml`
- `AzrngTools/Styles/DesignTokens.axaml`
- `TASK.md`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln -c Release`
- 未执行：`dotnet build AzrngTools.sln`
  - 原因：本地已有运行中的 `AzrngTools.exe` 占用 `bin\\Debug\\net10.0-windows\\AzrngTools.exe`

## 风险或遗留项
- 本次仅收紧首页 Hero 区域高度与 Padding，未调整首页其他卡片结构

