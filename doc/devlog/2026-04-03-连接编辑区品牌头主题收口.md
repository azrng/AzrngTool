# 2026-04-03 连接编辑区品牌头主题收口

## 本次目标
- 去掉连接编辑区头部对品牌色字符串的依赖
- 让连接配置编辑区头部徽标回到统一的工作台主题语言

## 核心改动
- 将 `ConnectionDialog.axaml` 中编辑区头部右侧徽标从品牌色背景/边框改为统一的 `connectionDialogBadge` 主题样式
- 为品牌简称和字标补充统一文本样式，保留数据库识别信息，但不再依赖品牌色差异
- 删除 `ConnectionDialogViewModel.cs` 中仅服务于旧头部徽标的 `CurrentBrandAccentColor`、`CurrentBrandSoftColor`、`CurrentBrandBorderColor` 属性及其通知

## 修改文件
- `TASK.md`
- `AzrngTools/Views/Database/Workbench/ConnectionDialog.axaml`
- `AzrngTools/ViewModels/Database/ConnectionDialogViewModel.cs`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln`
- 结果：通过，0 warning，0 error

## 风险或遗留项
- 本次保留了 `CurrentBrandMonogram` 和 `CurrentBrandWordmark`，仅移除了品牌色差异
- 如果后续还想进一步统一，已保存连接列表里的数据库类型图标底色也可以继续收口
