# 2026-04-01 发布包移除 pdb 文件

## 本次目标
- 在 GitHub Actions 打包发布前移除 `AzrngTools.pdb`

## 核心改动
- 更新 `.github/workflows/release.yml`
- 在压缩 `publish/win-x64` 目录前，先删除 `AzrngTools.pdb`
- 保证最终 release zip 中不再包含调试符号文件

## 修改文件
- `.github/workflows/release.yml`
- `TASK.md`
- `doc/devlog/2026-04-01-发布包移除pdb文件.md`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln -v minimal`
- 结果：通过，0 warning / 0 error
- 未执行：GitHub Actions 远端打包产物检查
- 原因：需要推送到 `main` 后检查 release zip 实际内容

## 风险或遗留项
- 当前仅移除 `AzrngTools.pdb`，其他符号或诊断文件仍按发布输出默认行为保留
