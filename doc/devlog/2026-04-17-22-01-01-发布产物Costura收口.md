# 本次目标
- 使用 Costura.Fody 收紧 Release 发布目录，尽量把可内嵌依赖并入主程序
- 保留桌面启动必需的原生渲染库，避免单文件发布后无法启动

# 核心改动
- 在 `AzrngTools.csproj` 的 Release 配置中恢复 `Costura.Fody`
- 新增发布阶段清理目标，从最终 publish 列表中移除 `Microsoft.Data.SqlClient.SNI.dll`、`msalruntime.dll`、`WebView2Loader.dll` 以及原生 `.pdb`
- 调整 `FodyWeavers.xml`，仅将可稳定内嵌的原生依赖交给 Costura，保留 `av_libglesv2.dll`、`libHarfBuzzSharp.dll`、`libSkiaSharp.dll` 作为外置运行时文件
- 更新 `README.md`，补充当前发布产物保留规则说明

# 修改文件
- `AzrngTools/AzrngTools.csproj`
- `AzrngTools/FodyWeavers.xml`
- `README.md`
- `TASK.md`
- `doc/devlog/2026-04-17-22-01-01-发布产物Costura收口.md`

# 校验情况
- 已执行：`dotnet publish AzrngTools\AzrngTools.csproj -c Release -r win-x64 --self-contained true -o publish\win-x64-costura-test-3 -p:PublishAot=false -p:PublishTrimmed=false -p:DebugType=None -p:DebugSymbols=false -v minimal`
- 已执行：启动 `publish\win-x64-costura-test-3\AzrngTools.exe`，进程可成功拉起
- 验证结果：测试目录最终仅保留 `AzrngTools.exe`、`av_libglesv2.dll`、`libHarfBuzzSharp.dll`、`libSkiaSharp.dll`

# 风险或遗留项
- `publish\win-x64` 目录里旧的历史产物未自动清理，本次验证使用的是新的 `publish\win-x64-costura-test-3`
- `Microsoft.Data.SqlClient.SNI.dll`、`msalruntime.dll`、`WebView2Loader.dll` 已改为通过 Costura 内嵌，当前仅完成发布与启动验证，相关功能页建议在下次发版前补一次人工点检
