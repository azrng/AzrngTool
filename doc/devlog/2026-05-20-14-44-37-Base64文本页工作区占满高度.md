# Base64 文本页工作区占满高度

## 本次目标
- 修复 Base64 文本页底部剩余空白过多的问题。
- 让左右输入与结果工作区随主窗口内容视口拉伸。

## 核心改动
- 移除 Base64 文本页内部 `ScrollViewer`，避免页面按固定内容高度结束。
- 将根布局改为 `Auto,*`，左右卡片内部编辑区使用星号行承接剩余高度。
- 在 code-behind 中复用 JSON 页同类做法，监听宿主 `ScrollViewer` 高度并设置根布局和工作区高度。

## 修改文件
- `AzrngTools/Views/Encode/Base64EncodePageView.axaml`
- `AzrngTools/Views/Encode/Base64EncodePageView.axaml.cs`
- `TASK.md`
- `doc/devlog/2026-05-20-14-44-37-Base64文本页工作区占满高度.md`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln`，构建成功，0 警告，0 错误。
- 已执行：重新读取修改后的 XAML、code-behind、`TASK.md` 与本开发记录，关键中文显示正常。
- 未执行：人工打开 Base64 文本页进行界面 smoke test；本次已按 JSON 页同类布局方案完成等价实现和编译校验。

## 风险或遗留项
- 本次只调整 Base64 文本页视图布局；需关注较小窗口高度下页面是否仍可通过主窗口宿主滚动访问完整内容。
