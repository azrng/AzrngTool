# 本次目标
- 将 Windows / PowerShell 下中文 Git 提交避免乱码的约束补充到 AI 规则文件

# 核心改动
- 更新 `AGENTS.md` 版本号与更新时间
- 在“编码与文档规则”中补充中文提交信息必须优先使用 `git commit -m` 或 `UTF-8` 文件配合 `git commit -F <file>`
- 在“编码与文档规则”中明确禁止通过 `git commit -F -`、here-string 管道等标准输入链路直接传递中文提交信息
- 在“Commit 规范”中补充提交前应确认终端与 Git 日志输出使用 `UTF-8`，并在提交后回读确认中文未乱码

# 修改文件
- `AGENTS.md`
- `TASK.md`
- `doc/devlog/2026-04-27-13-30-54-提交防乱码规则补充.md`

# 校验情况
- 已执行：重新读取 `AGENTS.md`
- 校验结果：新增规则已落位，关键中文显示正常，frontmatter 格式正常

# 风险或遗留项
- 本次只更新规则文件，未修改 Git 或终端全局配置
- 若后续需要彻底固化到环境层，还可补充 PowerShell UTF-8 初始化与 Git `i18n.commitEncoding` / `i18n.logOutputEncoding` 配置说明
