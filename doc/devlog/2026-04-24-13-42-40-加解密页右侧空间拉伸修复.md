# 本次目标
- 修复 `DES` 页面右侧工作区未占满可用高度的问题
- 同步收口 `AES`、`SM4` 等同类加解密页的右栏拉伸表现

# 核心改动
- 调整 `DesEncryptPageView.axaml` 右侧工作区行定义，让结果区占用剩余高度，操作按钮贴底展示
- 调整 `Sm4EncryptPageView.axaml` 右侧工作区行定义，让结果区拉伸填充右侧剩余空间
- 调整 `AesEncryptPageView.axaml` 右侧参数卡，移除仅顶部对齐限制，并让底部操作区贴底
- 补充 `.learnings/ERRORS.md`，记录本次构建锁文件与 PowerShell 检索转义问题

# 修改文件
- `AzrngTools/Views/Encrypts/DesEncryptPageView.axaml`
- `AzrngTools/Views/Encrypts/AesEncryptPageView.axaml`
- `AzrngTools/Views/Encrypts/Sm4EncryptPageView.axaml`
- `.learnings/ERRORS.md`
- `TASK.md`
- `doc/devlog/2026-04-24-13-42-40-加解密页右侧空间拉伸修复.md`

# 校验情况
- 已执行：`Get-ChildItem AzrngTools\Views\Encrypts\*.axaml | Select-String -SimpleMatch 'VerticalAlignment="Top"'`
- 校验结果：加解密视图中未再命中右栏仅顶部对齐配置
- 已执行：`Get-ChildItem AzrngTools\Views\Encrypts\*.axaml | Select-String -SimpleMatch 'RowDefinitions="Auto,Auto,*,Auto"','RowDefinitions="Auto,Auto,*,Auto,Auto"','RowDefinitions="Auto,Auto,Auto,Auto,Auto,*,Auto"'`
- 校验结果：`AES`、`DES`、`SM4` 已命中新行定义
- 已执行：`dotnet build AzrngTools.sln -c Release -v minimal`
- 校验结果：构建通过，`0` 警告，`0` 错误
- 未通过：`dotnet build AzrngTools.sln -v minimal`
- 原因：本地正在运行的 `AzrngTools.exe` 锁定了 Debug 输出文件

# 风险或遗留项
- 本次只做了结构和编译回归，未重新启动应用做人工视觉点检
- 如果还有其他工具页使用相同“右栏贴顶”布局但不在本次加解密范围内，后续需要继续巡检
