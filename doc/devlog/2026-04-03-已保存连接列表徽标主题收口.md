# 2026-04-03 已保存连接列表徽标主题收口

## 本次目标
- 将左侧已保存连接列表中的数据库类型徽标切回统一主题样式
- 移除 `ConnectionConfig` 中仅供旧背景色展示使用的字段

## 核心改动
- 在 `ConnectionDialog.axaml` 中为已保存连接列表补充统一的徽标样式，改用主题 token 渲染数据库类型简称
- 删除 `ConnectionConfig.cs` 中仅服务于左侧列表背景色的 `DatabaseTypeBackgroundColor`
- 保留 `DatabaseTypeIcon` 作为数据库类型识别信息，不影响已有连接列表辨识度

## 修改文件
- `TASK.md`
- `AzrngTools/Views/Database/Workbench/ConnectionDialog.axaml`
- `AzrngTools/Models/Database/ConnectionConfig.cs`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln`
- 结果：通过，0 warning，0 error

## 风险或遗留项
- 本次收口的是连接管理弹窗左侧已保存连接列表，不影响主工作台其他数据库图标展示
- 如果后续还要进一步统一数据库图标体系，可以再评估是否把 `DatabaseTypeIcon` 也抽成共享转换器或展示模型
