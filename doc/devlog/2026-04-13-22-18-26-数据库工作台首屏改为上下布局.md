# 数据库工作台首屏改为上下布局

## 本次目标
- 将数据库工作台首屏右侧概览区由左右分栏改为上下布局，减少横向切割感，让信息按阅读顺序纵向展开。

## 核心改动
- 将首屏 `dbWorkbenchHeroCard` 内部容器从双列 `Grid` 改为双行 `Grid`。
- 保持顶部标题、说明、当前连接和操作按钮为第一段内容。
- 将“工作台状态”和四个统计卡片整体下移到第二段，作为独立状态区展示。

## 修改文件
- `AzrngTools/Views/Database/DatabaseWorkbenchPageView.axaml`
- `TASK.md`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln -c Release -v minimal`
- 结果：通过，0 warning / 0 error
- 已执行：`dotnet test AzrngTools.sln -c Release --no-build -v minimal`
- 结果：命令执行完成
- 未执行：桌面界面人工回归
- 原因：当前终端环境未直接打开页面做可视化核对

## 风险或遗留项
- 这次只调整了首屏概览区布局顺序，未同步改动架构页和详情页；若后续希望三类页面完全统一，可再继续收口。
