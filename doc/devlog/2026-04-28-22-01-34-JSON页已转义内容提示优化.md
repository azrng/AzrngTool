# JSON页已转义内容提示优化

## 本次目标
- 优化 JSON 页对已转义内容重复点击“压缩并转义”时的提示信息，避免直接展示底层 JSON 解析异常。

## 核心改动
- 在 `CompressEscapeJsonCommand` 解析失败时识别已转义 JSON 文本。
- 对已转义 JSON 给出“先去除转义或直接使用当前结果”的可操作提示。
- 补充对应 ViewModel 单元测试，验证原内容不被覆盖且提示文案正确。

## 修改文件
- `AzrngTools/ViewModels/Format/JsonPageViewModel.cs`
- `AzrngTools.Tests/ViewModels/Format/JsonPageViewModelTests.cs`
- `TASK.md`

## 校验情况
- 已执行：`dotnet test AzrngTools.Tests\AzrngTools.Tests.csproj -c Release --no-restore`，通过 15 项测试。
- 未通过 Debug 测试启动：`dotnet test AzrngTools.Tests\AzrngTools.Tests.csproj --no-restore` 在编译阶段因现有 `WithDeveloperTools` 扩展方法缺失失败，未进入测试执行。

## 风险或遗留项
- 本次仅调整“压缩并转义”失败提示分支，不改变 JSON 压缩、转义和去除转义的成功路径。
- Debug 配置下 Avalonia 诊断工具链仍需后续单独处理。
