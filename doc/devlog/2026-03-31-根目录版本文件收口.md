# 2026-03-31 根目录版本文件收口

## 本次目标
- 将项目版本号收口到仓库根目录单一文件
- 让工程构建和 GitHub Actions 都从同一份版本来源读取

## 核心改动
- 新增根目录 `VERSION` 文件，作为版本号单一事实来源
- 更新 `AzrngTools/AzrngTools.csproj`，改为从根目录 `VERSION` 读取 `Version`
- 更新 `.github/workflows/release.yml`，改为读取根目录 `VERSION` 并比较前后版本
- 更新 `README.md`，将发版入口从修改 `csproj` 改为修改 `VERSION`

## 修改文件
- `VERSION`
- `AzrngTools/AzrngTools.csproj`
- `.github/workflows/release.yml`
- `README.md`
- `TASK.md`
- `doc/devlog/2026-03-31-根目录版本文件收口.md`

## 校验情况
- 已执行：`dotnet build AzrngTools.sln -v minimal`
- 结果：通过，0 warning / 0 error
- 未执行：GitHub Actions 远端实跑校验
- 原因：需要推送到远端后触发 workflow

## 风险或遗留项
- 以后正式发版只应修改根目录 `VERSION`，不要再手改 `csproj` 中的版本
- 如果未来引入多项目方案，需要评估是否继续共用这一份根目录版本文件
