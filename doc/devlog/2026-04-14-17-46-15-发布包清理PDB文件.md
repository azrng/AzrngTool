## 本次目标
- 调整发布链路，确保打包后的发布目录和最终 zip 中不再携带 `.pdb` 调试符号文件。

## 核心改动
- 在 `.github/workflows/release.yml` 的压缩步骤前增加递归扫描与清理逻辑，删除发布目录中的全部 `.pdb` 文件。
- 在压缩前增加二次校验；如果发布目录中仍存在 `.pdb`，则直接终止打包，避免调试符号继续进入发布包。
- 在 `TASK.md` 中登记并完成 `T086` 任务状态。

## 修改文件
- `.github/workflows/release.yml`
- `TASK.md`

## 校验情况
- 已执行：脚本与改动内容人工检查，确认清理逻辑位于 `Compress-Archive` 之前，且使用递归扫描覆盖发布目录。
- 未执行：完整 GitHub Actions 发布实跑、发布 zip 实物下载校验。
- 原因：当前回合按用户要求直接收口任务并提交，未等待完整远端发布流程完成。

## 风险或遗留项
- 当前清理逻辑已经覆盖工作流发布包，但仍建议在下一次正式发布后下载一次 artifact 或 Release zip，确认实际产物中不含 `.pdb` 文件。
